# memory.md — what the agent team has learned (self-updating)

> The agent appends dated learnings here after planning runs, post-mortems, and
> experiments. Newest first. Humans may edit/prune freely. Keep entries short:
> one finding, one line of evidence, one implication.

## 2026-06-28 — color engine "all four" complete + a bought Remotion blueprint was 90% redundant

- **Color engine — Elijah's "all four" quality axes are DONE** (DRAFT/uncommitted): item 2 scopes `--validate`
  (falsifiable grades + JSON sidecar), item 3 G1b skin-LOCAL exposure (a **bakeable colour-qualifier** lift —
  flicker-safe because it's a function of colour not pixel position; raised real derwin skin 22→37 IRE), item 4
  `--look auto` widened to all 10 looks (verified on real footage — moody_blue fires on night shots). 31/31
  pytest. Remaining = Elijah's calls (pick favourites / wire live AI picker / commit). Baton
  `SESSION-COMPACT-2026-06-28.md`. **Lesson: `--validate` (the ruler) is what *surfaced* the item-3 gap — build
  the falsifiable check first and it tells you what to build next.**
- **A purchased 3rd-party "Claude + Remotion" course was ~90% stuff we already do better.** A workflow
  adversarially compared each technique vs our real on-disk skills; the genuinely-new bits were 4 small things
  (two reusable production prompts + two disciplines), harvested into one new skill `remotion-scene-director`.
  **Lesson: when evaluating someone else's "blueprint/skill pack," grep our ACTUAL skill files (adversarial,
  default-to-"we-have-it") before implementing — most of it is usually already covered; keep only the de-duped nuggets.**
- **`.md` files in a bundle can be lying about their type:** two "SKILL.md" files were actually a DOCX (zip) and a
  PNG. **Sniff the magic bytes (PK / \x89PNG) before parsing, not the extension.**

## 2026-06-26 — color-masterclass deep analysis (Pass 1 of "full everything") + multi-agent cost/cap lessons

- **Scope + result:** completing the FULL vault color-grading source list (82 videos) beyond the original 14, in staged vision passes. **Pass 1 = 11 individual masterclasses fully extracted → 2,977 techniques → all 11 per-video sheets written** to `docs/research/color-masterclass/per-video-v2/`; only the final MERGE into `synthesis_v2.md` is left. **Passes 2 (25 Hindi) + 3 (46 DR20) = 71 videos downloaded + dense + batched (245 batches) ready.** Cross-env handoff: `NEW-ENVIRONMENT-HANDOFF.md` (root).
- **COST LESSON (big):** one large vision pass ≈ a FULL usage window. Pass 1 = ~24–27M subagent tokens / 416 agents / ~42 min and **exhausted the session cap twice on the final step.** Plan **one large pass per usage window**; cost ≈ total kept frames (vision = image-token-bound), so cut cost by raising dedup `--hamming`, NOT by changing batch size.
- **Workflow resume is NOT a safety net here:** (1) cross-session/cross-account resume cache is unavailable (journal is local under `~/.claude`), and (2) even same-session it **missed on the concurrent pipeline and re-extracted.** **Fix: persist the expensive output to DISK as it's produced (per-video sheets), then finish with a cheap merge-only pass that reads the sheets — never rely on re-running extraction.** Built `merge-only.workflow.js` for exactly this.
- **`Workflow` args footgun:** an args object can reach the script as a JSON **string** → `args.videos` undefined → the workflow silently processes 0 items. **Always `JSON.parse` args if `typeof === 'string'` and throw on empty.** (First Pass-1 run wasted because of this.)
- **yt-dlp bulk-download hardening (reusable for any acquisition):** the standalone `yt-dlp.exe` hit persistent sub **429s** (no impersonation) + video-data **403s** (no JS runtime). Fix: `pip install -U yt-dlp curl_cffi` into the venv + Node on PATH, then `--impersonate chrome --js-runtimes node`, and **decouple video from subs** (a sub 429 must not cost the video). Took playlist acquisition 44/71 → 70/71. Some videos still sub-429 → analyze FRAMES-ONLY (stub a `WEBVTT` .vtt so the dense step doesn't skip them; the vision pass reads frames directly anyway).
- **GOTCHA:** `acquire_dense.py` grabs the FIRST `*.mp4` in a dir — leaving a stray audio-only `.mp4`/`.m4a` next to the real video makes it extract 0 frames (it tried the audio file). Clean temp audio files before dense.

## 2026-06-25 — reverse-engineered a viral reel → Headroom carousel (AI+Money+builder triple-intersection)

- **Rode a proven reel** (`instagram.com/reel/DZ8OVCnCxCo/`) selling the free OSS tool **Headroom** (compresses logs/files/DB-dumps/RAG before they hit the LLM) → rebuilt as a 9-slide SAVES carousel in his voice. Vault: `20-Content/Ideas/Your AI Bill Is Mostly Garbage Tokens - Headroom Carousel.md`. DRAFT only. Save-magnet slides = "how to start" steps + "what it strips" examples; money hook drives sends. Topic sits dead-center AI/Tech (best save-rate) + Money (reach leader) + his real builder identity.
- **Pipeline that worked (reusable):** `yt-dlp` → local mp4 → `video-analyzer` MCP `analyze_video` (detail=detailed). **GOTCHA: IG reels often have NO caption track** ("No transcript available") — so the script came from OCR + the burned-in word-by-word captions visible in the extracted FRAMES, not a transcript. Read the frames.
- **CLAIM-TIERING saved us from parroting hype** ([[feedback_primary_source_platform_claims]]): the reel's "$700k saved / 31k stars / bill $1,240→$180" were stale/mockup numbers. Verified via **GitHub API** (primary source): real repo, **~51.2k★, Apache-2.0, created Jan 2026, pushed today** → used "51k+ stars," tagged "60–95% fewer tokens" as the *repo's own claim*, and DROPPED the $ figures. **Lesson: before Elijah recommends a specific tool to 100k followers, verify the repo + tier every number; recommending an install is a real-world claim, not just a draft.**
- **FRAMING feedback (reusable):** Elijah rejected the first pass as feeling "like an ad." For a tool/feature carousel he wants the **peer "I found this insane free tool, putting you on, use it yourself"** voice (info on what-it-does lives in the MIDDLE slides; CTA = save + send, no lead-gen bait), NOT a product pitch. Signoff "be smart be safe be blessed." **Lesson: position external tools as a cheat-code you're sharing, never as marketing.**
- **CAROUSEL DESIGN BAR + render pipeline (reusable, beats flat thumbnails):** his liked decks (`carousel-builds/claude-runs-ig/`) are HTML→PNG with **radial-gradient depth + premium type pairing (display+Inter) + glass cards + eyebrow labels + accent spans**, NOT flat CSS boxes (he rejected those + the constrained `visualize` widget previews as low-quality). Repro: `carousel-builds/headroom/build.py` = shared STRUCT css + content + **theme vars** (frost light / aqua dark) → emits 8 slide HTMLs + a contact-sheet board → `render.ps1` screenshots via **headless Edge**. **GOTCHAS:** (1) Edge is a GUI app → must use `Start-Process -Wait`, not `&` (else it doesn't wait / no exit code); (2) add `--virtual-time-budget=4500` so Google Fonts load before the shot; (3) **`--screenshot=<path with spaces>` breaks with "Multiple targets are not supported"** → screenshot to a `$env:TEMP` (no-space) path, then Copy-Item to the final `ai agent team/...` path. Preview at the REAL render (board PNG), reskin = edit THEMES + re-render (copy untouched). **VISIBILITY lesson (Elijah pushed back twice):** background textures at ~.05–.08 opacity are sub-pixel and VANISH after IG compression ("had to zoom all the way in to see it") — go bold, ~.30–.40 opacity + 2px lines, and judge at full-res, not board scale. Final: Frost = visible teal graph-grid + dual corner glows; Aqua companion = reworded copy + radar-ring arcs (different shape language per deck). Per-theme copy via `content_for(theme)` in build.py.

## 2026-06-25 — resilient overnight ideation FINISHED (the rate-limit fix worked) + a checkpoint gotcha

- **The resilient relaunch worked.** `docs/plans/overnight-ideation-resilient.workflow.js` completed clean — **147 agents / 9.85M tokens / ~3.3h, NO session-cap blowup**, where the prior deep run died mid-flight at 271 agents. **Lesson confirmed: the cap fix wasn't "wait for reset," it was SCOPE — skip re-research (read the finished `team/state-of-play.md`) + bound the loop (max 6 rounds, stop after 2 dry).** Output: 582 ideas → critiqued → 93 adversarially vetted → 28K-char compiled bank. Saved to `docs/plans/2026-06-24-content-idea-bank.md` + raw `*.raw.json` + vault `20-Content/Ideas/2026 Content Idea Bank.md`. DRAFT only.
- **GOTCHA — per-round "rewrite the whole growing doc via an agent" checkpoint hits the 64k output-token cap.** `[checkpoint:r3]` failed: by round 3 the full draft was too big for one agent to re-emit verbatim (Write-agent response > 64000 output tokens). It didn't cost us anything (the returned result object carried ALL ideas + the compiled doc), but **the checkpoint pattern is the weak link. Fix next time: checkpoint by appending only the round's NEW ideas, OR write the file deterministically in the workflow script body (no agent), OR rely on the final result object (what saved us).** Don't have an agent rewrite an ever-growing document.
- **GOTCHA — the workflow result file wraps the return under `result`.** The task `.output` file (914K chars) had top-level keys `summary/agentCount/logs/result`; the `{brief,compiled,ideas,...}` payload is under `data["result"]`. **Parse big workflow outputs with a throwaway script (json.loads → extract `compiled`) — never dump 900K chars into context.** Console mojibake (`�` for ×/—/emoji) was just PowerShell's cp1252 stdout; the utf-8 file write was clean (verified by re-Reading).

## 2026-06-26 — vault auto-linking + DaVinci Resolve Studio install fix

- **Vault connectivity pass (Obsidian):** Elijah's quick-capture notes were orphans — notes Claude authored were densely `[[linked]]`, his fast brain-dumps had ZERO links. Built a safe ADDITIVE linker (`_vault_linker.py` at hub root): backed up the vault first (not git-tracked → zip `obsidian/_vault-backup-link-pass-2026-06-26.zip`), created 5 missing HUB notes that many notes linked to but didn't exist ([[Tanner]], [[ClipWith]], [[Artifacial]], [[Infinet]], [[Labeltrust]] = broken links), then appended `## Related` sections to 38 notes — never touching frontmatter/prose, never deleting, validated every link resolves. **Lesson: for a personal vault, reason about relatedness with the FULL index in ONE pass (global dedup across files) + apply DETERMINISTICALLY via script (not agent free-edits, which risk mangling personal notes); a broken `[[X]]` usually means a missing hub note → create the hub, don't drop the link.** Inline-mention linking (prose → links) offered but held — it edits prose, needs his explicit OK.

- **DaVinci Resolve Studio install — "error reading from file …Temp\{GUID}\DaVinciResolveOpenFXRenderer.msi":** consistent failure on the SAME child MSI. Root cause = a **leftover DaVinci Resolve 21.0 install** conflicting with the Studio installer (also: he ran it from `OneDrive\Desktop`, and the 2 MB bootstrapper `.exe` needs its ~9.6 GB `.dat` payload extracted ALONGSIDE it). **Community-confirmed fix (Blackmagic forum accepted answer): Microsoft "Program Install and Uninstall troubleshooter" → Installing → DaVinci Resolve → repairs the broken MSI registration; projects preserved (they live in a separate DB).** What unblocked Elijah: extract the zip to a clean LOCAL folder (`C:\ResolveInstall`, both files together) + clear the old install → install succeeded. **Lesson: never run installers from OneDrive-synced folders; "error reading from a Temp .msi" = broken prior-install registration or AV grabbing the file, NOT a corrupt download — confirm via installed-programs list + whether the temp file is a chained child MSI.**

## 2026-06-25 — "Introducing ClipWith" overnight edit-prep (footage → edit-ready package)

- **Built the edit-prep pipeline end-to-end:** 24 raw takes → whisper.cpp word-level transcripts → keeper-take map → 20 frame-accurate trimmed clips + `Introducing-ClipWith.fcpxml` (Resolve auto-timeline) + assembly EDL + 6 on-brand motion graphics + SFX prompt specs. All in `clipwith-edit/`. DRAFT, never published. (Resumed from `docs/plans/2026-06-25-overnight-clipwith-edit.md`.)
- **Footage was in the WRONG folder** (`clipwithedit/` no-hyphen, not `clipwith-edit/source/`) — evidence (24 dated VID_ takes + Røde backups) was unambiguous, so I proceeded + flagged rather than blocking. **Lesson: when the runbook's input path is empty, look one folder over before declaring blocked.**
- **GOTCHA — rotation flag ≠ orientation:** ffprobe reported the early files as 3840×2160 ("landscape") and I almost shipped "reframe the landscape shots" as the #1 decision. WRONG — they carry a rotation flag; ffmpeg bakes it on re-encode → **2160×3840 portrait**. Every clip is 9:16. **Lesson: check `rotation`/display matrix (or just trim one + look) before trusting stored stream WxH; verify visually with a frame grab.**
- **GOTCHA — whisper.cpp install dies on space-paths:** `@remotion/install-whisper-cpp` runs `Expand-Archive` UNQUOTED, so "abc wrap"/"ai agent team" (spaces) break both the unzip and whisper-cli. **Fix: install to a space-free path (`C:/Users/elija/.cache/whisper-clipwith`) and pre-extract the zip yourself (quoted) so installWhisperCpp sees `main.exe` and skips its broken step.** base.en on the clean Røde WAVs = accurate enough (got "trafficking marijuana", "$200 billion", names).
- **Keeper-take heuristic held + needed GLOBAL dedup:** keeper = the LAST clean take in a file (director says "that's the one"; bloopers/profanity live in earlier takes — esp. the long run-throughs 201/204/206/209). But the SAME line recurs across files (175 vs 176, 179 vs 180, 188 vs 189) — picking the best requires seeing ALL transcripts at once, so I did identification INLINE (not blind per-file fan-out). **Lesson: per-file fan-out is wrong when takes duplicate across files; one global pass dedups correctly.** Missing beats flagged (no clean "ClipWith.ai" name-drop). Re-used abc wrap's existing ClipWith brand system (`pitch-script.ts` BRAND + `LogoReveal`) for the graphics → solved the missing-logo gap.

## 2026-06-25 — color engine: the masterclass research was sparse; redone frame-by-frame + integration started

- **HONEST AUDIT (Elijah challenged it):** the original color masterclass "deep analysis" was NOT exhaustive — `acquire.py` took **50 evenly-spaced frames per video** (1 frame / ~2 min on the long ones; ~700 frames / 7.7 h) + a 15s-window transcript, never time-fused. So `synthesis.md` was strong *theory* but its numbers were textbook, not read off the demonstrations. **Lesson: "I watched 14 videos" ≠ exhaustive — quantify sampling density before claiming frame-by-frame; for color, the on-screen scope/wheel values ARE the signal and sparse sampling misses them entirely.**
- **FIX (validated):** `docs/research/color-masterclass/acquire_dense.py` — 1fps → dHash perceptual-dedup (keep only frames where the screen changed) → **word-level** transcript fusion (overlap-merge the rolling VTT). All 14 → **2,498 distinct timestamped frames** (vs 700), legible at parameter level. Then a Workflow (130 batch vision-agents, 123 ok) → **1,363 frame-cited techniques** → `extracted/synthesis_v2.md` + `engine_gap_map.(json|md)` (3×P0/4×P1/8×P2/2×P3) + `per-video-v2/*.md`. The synthesis **validated** existing colorkit calls (linear 0.18 pivot is correct, NOT 0.435 which is log/encoded space; look-under-grade @0.7; luma-safe `apply_to_luminance`; match-alpha 0.8) and self-flagged 5 "demonstrated" numbers as Resolve-Offset-scale confusions to re-check.
- **INTEGRATION (measurement layer, pure-additive, develop untouched):** new `colorkit/scopes.py` (headless waveform/parade/vectorscope + `validate()` vs demonstrated targets) + `measure.skin_signature()` (skin hue vs the 123° I-line + IRE). **Tested on the real talking-head:** develop fixes milky blacks (13.8→2.6 IRE, 10/10 targets) **but drifts skin hue +7.7° further off the I-line (129→137°)** — the #1 (P0) gap, now demonstrated not asserted. **Lesson: build the ruler (scopes) before the adjustment (skin solver) — measurement-first makes the next change falsifiable.** Next: G1 skin-gated correction in `correct.py`. Baton: `auto-clip/COLOR-BUILD-STATUS.md` (2026-06-25 §). DRAFT/local. Memory: [[project-color-engine]].

## 2026-06-25 — session consolidation: analytics reality, inbound-deal vetting, reel-recreation pipeline, storage project

- **ANALYTICS REALITY (price + strategy off THIS, not the headline):** "9.9M views/51d" is **outlier-driven** — recent **median reel ≈ 2,200–3,000 views** (last 10 / last 25); the millions came from a few May virals (top reel 3.95M ≈ 40% of all views). **June cooled hard vs May** (May 8.49M views/160 posts → June 1.38M/135). **~90.6% of views = non-followers** (discovery engine). Money/Finance = efficiency king (147K avg/post); Motivation most-posted, lowest return (7.6K/post); **0 carousels** (format gap). 3-mo recap + Higgsfield infographic prompt: `ig-dashboard/3month-recap/{DATA,PROMPT}.md`. **RESOLVED — why June reach dropped:** it's DISTRIBUTION, not content. Per-view signals held FLAT May→June (watch 10.7→10.6s, skip 45.9→46.1%, share rate ~1.5%, save rate 0.35→0.51%) but **median views/post fell 57% (10,035→4,305) across EVERY category** (account-level, not topic). #1 suspect: he **~doubled posting frequency** (~4.4→~8.1 posts/day) → over-posting dilutes per-post reach + soft cooldown (median bled all month: May ~10K → early-June ~5.4K → late-June ~3.4K). Fix (ranked): cut to ~2-4 BEST posts/day for 2 wks (test), stop flooding low-return Other/Motivation filler, concentrate Money+AI, engineer more shares (the non-follower lever), try carousels (0 so far). If no recovery in 2wks → check for a triggering post / originality demotion. **Timing (his data):** best days **Thu/Fri/Sat** (Tue weakest); best publish windows **~11am ET** (audience most online) + **6–10pm ET** (top performance); audience-online peaks midday UTC (US-morning/UK-afternoon/AU-evening blend). Executable 2-week reach-recovery test plan → `ig-dashboard/reach-recovery-plan.md` (≤3 posts/day, concentrate Money+AI, cap Motivation filler, 3 carousels/wk, design for shares; agent re-measures median in 14d). **Proven hooks + draftable hook bank → `ig-dashboard/hook-bank.md`:** highest-SHARE lane = comedy/"Other" reactions (829K/**41K-share** "ain't no way"; 209K/12.9K "folded faster than laundry") = the non-follower rocket — but that's the lane he over-flooded as filler, so keep them SHARP not many. Molds: watch-the-whole-thing retention bait · disclaimer-as-hook ("I do NOT condone") · hidden-truth reframe ("exit liquidity") · comment/DM-a-keyword lead-gen · "RICH RICH if you lock in" FOMO. Signoff: "be safe be smart be blessed."

- **INBOUND DEALS / SCAMS (he's getting them now; reusable playbook → auto-memory `reference-inbound-deal-playbook`):** (a) **Tec-Do/Dreamina** sponsorship = real agency+brand but "mixed-reports" PAYMENT RISK (non-payment allegation; offer diverges from Dreamina's official credits-not-cash program) → payment-first protections, refuse perpetual, never download "campaign asset" files (malware). (b) **"Publicis / Chief of Social Media" email = CONFIRMED scam** (lookalike domain `publicisgroupepeople.com`, on BBB Scam Tracker; real = publicisgroupe.com) → don't reply. **Rate reality:** ~100K-but-low-median profile → IG reel **~$1,500–2,000** (NOT $3K+ — agencies check views); put value in usage/whitelisting (view-independent); bundle ~$3–4K; never give perpetual free.

- **REEL-RECREATION PIPELINE (reusable):** `yt-dlp` (`C:\Users\elija\.local\bin`) downloads IG reels → `video-analyzer` MCP `analyze_video` on the local mp4 (frames+OCR+caption) reads the format → shot-by-shot brief. Library + briefs in `abc wrap/reference-reels/`. ClipWith launch script: vault `20-Content/Scripts/Tanner and I.md` (frontmatter + shooting script added this session).

- **GEAR:** Røde **Wireless PRO** — 32-bit float is automatic but **only on the TX on-board recording** (set "Always" in RØDE Central; the camera feed is NOT 32-bit; use the internal WAVs in post). Luna Ultra → auto-memory `reference-insta360-luna-ultra`. **Storage pain:** 1TB internal (FULL), 20TB Seagate Expansion (archive), ONE 512GB SanDisk Extreme PRO V60 — ~200 videos/mo, 100-200GB+/session, 50-100GB+ per 8K Luna clip.

- **STORAGE SYSTEM RESOLVED → `docs/media-storage-system.md`:** Missing tier = a fast **working SSD** (4TB ext NVMe Crucial X9 Pro, or internal NVMe) to edit off; 20TB Seagate = archive; add a 2nd HDD offsite + Backblaze ($99/yr) = 3-2-1. **SD CATCH: his V60 card is UHS-II = wrong for the Luna (UHS-I microSD slot); use UHS-I V30 A2, get 2 for rotation.** IG hard-caps at 1080p (control the downscale in Resolve; 8K master pays off in reframe/YouTube/agency delivery via B2/Wasabi buckets, never email/IG). Offload = TeraCopy verified COPY → SSD → mirror to Seagate → format card in-camera. Key truth to deliver: "**no quality loss over time**" is a myth — copying is bit-for-bit lossless; only **re-encoding** or **drive failure/bit-rot** lose quality (→ that's a backup tier, not a compression problem). Vault adjacents: `To do general/Clipping.md` (deliver uncompressed clips to an agency) + the "unlimited free cloud" reality check.

## 2026-06-24 — color engine: 5 fresh film looks added (10 total) + lookbook

- **Added `golden_hour`, `moody_blue`, `bleach_bypass`, `clean_pop`, `portra_style`** (now 10 looks) in `luts.py`, each designed in the v2 idiom: the develop owns exposure + contrast, so a look carries only HUE + DENSITY character (split-tone + a whisper of density, never a re-stacked s-curve) and runs UNDER the grade at 0.7 = tasteful, not a one-tap filter. Distinct moods covering his content: warm/aspirational, cool/night (fits his night driving footage), gritty, punchy-commercial, soft-portrait.
- **Method that worked: build a labeled lookbook and EYEBALL it.** Generated all 10 onto one developed talking-head frame (`out/ftest/hdr/_LOOKBOOK_v2.png`) + a larger 5-up of the new ones — confirmed each reads as its intended mood, none broken/over-cooked, skin stays reasonable. Implication: for creative/look work the acceptance test is a side-by-side render you actually look at, not unit tests. New looks are manual-select; `--look auto` still uses the curated original 5 (didn't re-tune the verified picker).

## 2026-06-24 — color engine: develop hardened for dark/mixed-light scenes (gated, zero regression)

- **Fixed the one real quality issue task #8 surfaced** — the night-driving clip developed **green-yellow** (gray-world WB over-warmed mixed artificial light + the median→0.18 exposure slammed its 4.0× ceiling, amplifying the cast). Fix in `correct.py`: two automatic guards that engage ONLY when a scene *demands a large lift* (it's genuinely dark) — an **exposure soft-knee** (lifts ≤2× untouched; above rolls off toward ~3.2× instead of clamping at 4×; night 4.0→2.93×) and a **low-light WB fade** (gray-world fades toward neutral where it's least trustworthy; night blue-gain 0.70→0.85). Plus a manual **`--strength S`** knob (0..1, default 1.0) scaling the whole develop toward identity.
- **The discipline that made this safe: gate any change to an APPROVED core on a difficulty signal, then prove the normal case is untouched.** Elijah signed off on the v2 develop for well-exposed footage; a naive "make exposure gentler" would have regressed that. Instead the guards key off how much lift the scene demands, so a well-exposed frame is a no-op — verified by a **0-pixel diff** on the daytime develop (old vs new) before/after the change. Lesson: when refining a tuned, user-approved algorithm, don't change the global behavior — detect the failing sub-population and fix only there, and regression-test the approved case to byte-identity. Proof: `out/ftest/hdr/_NIGHT_develop_fix.png`.

## 2026-06-24 — color engine task #8 stretch goals (HDR tonemap · GPU decode · auto-look)

- **All three stretch goals built + footage-tested** (hub copy `auto-clip/colorkit/`; standalone repo NOT diverged). Three focused new modules (`hdr.py`, `accel.py`, `decide.py`) + thin `color.py` wiring + `measure.scene_stats`. New CLI: `--hdr-tonemap`, `--hwaccel`, `--look auto`. All no-ops on plain SDR clips, so existing behaviour is unchanged.
- **HDR was the real gap.** His Luna HLG clips (bt2020/`arib-std-b67`, found 4 of them) grade **washed** if decoded as SDR — proven by eye (naive frame is milky/lifted-black). Fix = a **Stage -1** zscale+`tonemap=hable` (CPU, deterministic) HDR→Rec.709 pass BEFORE the develop, applied to the rep frame too (measurement parity). Daytime montage confirms full recovery. This ffmpeg (**Gyan 8.1-full**) has zscale+tonemap+**libplacebo/Vulkan** (BT.2390 opt-in) — no extra deps needed.
- **The 8K pipeline is DECODE-bound, not encode-bound — a measured surprise.** NVDEC (`-hwaccel cuda` on the RTX 5070) cut pure decode **7.7s→1.9s (4×)**, but end-to-end only ~15% (the signalstats probe + scenedetect + x264 encode are the rest). **NVENC tested and REJECTED:** identical wall time to libx264 *and* 79% bigger files — adding it would've been a pure regression. Lesson: benchmark before "obvious" GPU swaps; the bottleneck wasn't where the status note guessed ("decode would cut wall-clock a lot"), so decode-only hwaccel + keeping libx264 is the right call.
- **Auto-look = deterministic brain now, AI brain sketched.** `scene_stats` (6 cheap signals) → `decide.suggest_look` picks a look (skin→warm_interview, foliage→fuji, warm→kodak, cool→teal_orange). Real talking-head → warm_interview ✓. YCrCb skin detection fires on warm surfaces (tan asphalt) — documented limit that the **AI brain** (`suggest_look_ai` stub; Claude-vision-agent provider, "Claude IS the brain", per the design doc) is meant to fix; wiring a real model stays Elijah-gated. Implication: ship the conservative heuristic as a defensible default, gate the paid/external intelligence.

## 2026-06-24 — long autonomous workflow hit the account session cap; resilience pattern logged

- **The deep overnight content workflow (271 agents / ~14M tokens / 95 min) hit the ACCOUNT session limit mid-ideation** (resets on a clock, was 7:10am ET) — rounds 5-7 + final compile died. Two design bugs made it worse than it should have been: (1) it **re-ran 5 rounds of web research that were already done** (state-of-play.md existed) — the expensive, slow half; (2) it **only checkpointed the research brief and did NOT return/persist the generated idea objects** → hundreds of ideas were lost (only in-memory + scattered subagent transcripts).
- **Resilience pattern for any multi-hour workflow (now baked into `docs/plans/overnight-ideation-resilient.workflow.js`):** (a) SKIP already-finished expensive phases — read the saved artifact instead of re-researching; (b) checkpoint a FULL usable deliverable to disk EVERY round (deterministic markdown → low-effort Write agent), not just titles; (c) RETURN all accumulated data in the result so nothing's lost if compile fails; (d) budget-gate the loops (`budget.total ? budget.remaining()>RESERVE : roundCap`) so a "+Nk" launch self-throttles; (e) wrap compile in try/catch → fall back to the live draft.
- **Hard facts about limits:** an account session cap **cannot be waited out inside one run**, and Workflow **resume is SAME-SESSION-ONLY** (dead after `/clear`). So cross-session continuity = **disk checkpoints + a lean budget-gated design + relaunching when the budget is fresh**. Handoff for the relaunch: `SESSION-HANDOFF-2026-06-24-overnight-research.md`.

## 2026-06-24 — State of Play extended to 29 sections (full verified-research fold-in)

- **Grew `state-of-play.md` from 18 → 29 sections** with all verified frontier research, no regression. New: §19 audio-as-offense (audio-page tap is a NAMED Reels signal `[platform-official]` — original sound is a lever, not just the API constraint), §20 hook/loop/pacing craft gates (5–7 visual changes/10s; intentional loop on ≤15s clips; multi-layer hook in ~1s), §21 reel-cover gap (grid is **3:4 / 1080×1440** since Jan-2025 Mosseri — the old "1:1 safe zone" is OUTDATED; `auto-clip` has NO cover module — confirmed in code), §22 caption-SEO (his real lever is caption SUBSTANCE — recomputed word-count~reach **+0.42**, hashtags~reach **−0.11/noise**; he's NOT hashtag-heavy; Google indexes public posts since Jul-2025), §23 Broadcast Channels (free owned-audience spine, no 200/hr cap, in-app-only), §24 course→AI-agent pivot (Archetype = #1 move), §25 comment-flywheel (+21% is CURRENT-post only; the "23% on future posts" claim does NOT verify), §26 AEO/GEO (living comparison hub; quotes/stats/citations win, keyword-stuffing loses), §27 faith-as-economic-asset (peer-reviewed: identity cues +engagement, promo cues −engagement), §28 newsjacking two-clock system (story decay in HOURS, IG post distributes ~18h), §29 long-form funnel math (subscribers are a RESULT not the reach lever; "valued watch time" is the objective).
- **Key corrections baked in:** the "65% more plays from custom thumbnails," "30%/2× keyword-caption reach," and "23% future-post engagement from first-hour replies" stats are all UNVERIFIABLE orphan claims — deleted as fact, flagged for A/B. Finance long-form RPM corrected DOWN to ~$10–29 (was inflated "$28–40").
- **Implication:** 13 new Elijah-gated follow-ups (§18 #11–17) — highest-leverage zero-cost: ship carousels + per-clip cover, stand up one Broadcast Channel, pivot Archetype course→agent. Every marketer magnitude still tagged + gated to his own 300-post history.

## 2026-06-24 — color engine: footage-tested, `--height` added, v2 quality pivot, vault memory wired

- **Footage-tested the engine on Elijah's real Luna Ultra footage** (`OneDrive\A footage`): stills ✅, normal video ✅ (multi-shot + match), I-Log 8K ✅ (generic correction develops flat Log decently), HLG HDR ⚠️ (decoded as SDR, no tonemap — fine on dark scenes only), 8K ✅ via a NEW **`--height N`** delivery-downscale flag (8K → 1080×1920 IG-ready, rotation auto-handled; 8K *decode* ~10× realtime is the bottleneck). Robust on spaces-in-filenames + rotated footage.
- **Elijah's quality verdict: v1 looks "overwashed/overblown" — a tone change like Instagram's Edits app, NOT a real grade. He's right.** Root cause (confirmed by his own color tutorial [order-of-operations] + a grade-craft research workflow): **wrong order + exposure over-lift (pushing mean luma to a fixed ~0.46) + contrast stacking (correction contrast + a contrasty LUT) + no highlight protection → clipped dynamic range = the washed one-tap look.** Lesson: auto-correct + a creative LUT is NOT grading — the "deterministic 70%" only looks pro if you respect tone-mapping (filmic shoulder, middle-grey anchor, look applied AFTER a balanced base, output-transform LAST).
- **Official Insta360 I-Log LUTs now in hand** (Elijah provided) → copied to `auto-clip/colorkit/input_luts/` (Rec709 BT1886 = SDR input transform — use this; HLG/St2084 = HDR future). v2 applies the Rec709 LUT FIRST for Log footage.
- **DECISION: the color engine becomes its OWN separate repository** (Elijah's call) — extracted AFTER the v2 quality overhaul. Copy-first (hub stays intact), no push.
- **Obsidian vault now wired as durable project memory**: `40-Projects/LarpSlop/Completed/` ([[Completed — master log]] + [[Color Engine (colorkit)]]) + [[Working on]]. Pull from these for cross-session project state, alongside `team/memory.md` + the hub `SESSION-COMPACT-*.md`.
- Tooling: `gemini-video` MCP `analyze_tutorial_video` reads YouTube tutorials directly (great for mining the vault's masterclass links); it 400s on very long videos (>10800 frames for gemini-2.5-flash).

## 2026-06-23 — agentic color grading: build our own headless engine, don't buy DaVinci Studio

- **Triggered by Elijah drowning in DaVinci Resolve free.** First finding (direct research): "agentic DaVinci" MCPs exist + support Claude Code (samuelgursky etc.) but **ALL require Resolve STUDIO ($295), not free** — the free edition blocks the external scripting API (repo's own words: "The free edition does not support external scripting"). AND even on Studio the API can't truly grade — no wheels/curves/nodes, only apply pre-built LUTs/grades. So agentic-Resolve = $295 for nothing past the free LUT drag. Verdict logged in `AGENT-TEAM-BLUEPRINT.md` (2026-06-23).
- **The real answer = build our OWN headless Python+FFmpeg color engine, no Studio.** 7-lane research workflow (28 agents, 9/10 load-bearing claims survived 2-vote verify). Full plan: `docs/plans/2026-06-23-agentic-color-pipeline.md`.
  - **Most per-pixel color data = OpenCV+NumPy** (raw H×W×3 arrays, superset of every aggregate), NOT a cloud service. `colour-science` = perceptual targets (ΔE2000/CCT); FFmpeg `signalstats`→ffprobe JSON = cheap whole-clip-per-frame triage. **Gemini/TwelveLabs return ZERO numeric per-pixel color** (Gemini docs confirm boxes/masks only; Pegasus descriptive only; Marengo retrieval) — they're the DECISION layer (pick the look), never the MEASUREMENT layer. Only Google Cloud Vision `image_properties` crosses over (dominant RGB).
  - **Architecture = pro order: 1 correct each shot → 2 match shots → 3 stylize last.** Non-negotiable video rule: **never per-frame-independent correction → flicker/pumping**; analyze ONE rep frame per shot → ONE constant correction → apply across the shot (constant params = structurally no flicker). PySceneDetect segments; FFmpeg applies; `deflicker`/`normalize` backstop.
  - **~70% of pro color work is fully scriptable at near-pro quality today.** Ceiling only at (a) AI shot-matching (Colourlab.Ai / Resolve Color Match = GUI-only, no API) and (b) hard semantic masks (sky/skin) — quality gaps, not capability gaps.
- **Reuse, don't rebuild:** grounded against real code — `auto-clip/` is already ffmpeg+OpenCV CLI stages, stops at `out/`, and **already has PySceneDetect on its roadmap**; the new engine drops in as `color.py` (after `tighten`, before `caption`). `abc wrap` `color-grade-system` skill = the look library (CSS/Remotion) whose looks we export to .cube so both stay in sync. Lesson: when a creative skill (grading) feels like it needs a pro GUI app, check whether the *deterministic 70%* is just pixel-math + ffmpeg you can automate — it usually is, and the GUI only owns the brittle semantic 30%.
- **Prior-art check (Elijah asked "surely this exists already" — 2nd workflow, 25 agents):** PARTIALLY. The *concept* (auto-correct + cinematic grade, even AI shot-match) ships everywhere (CapCut, Google Photos, Colourlab AI, fylm.ai, DaVinci). But the *exact box* — HEADLESS + true single-file per-shot anti-flicker segmentation + measured-neutral + creative stylize-on-top + agentic look-choice, as a CLI/API — is **unshipped** in both commercial + OSS. Closest runnable = **`perbhat/agentic-color-grader`** (MIT, LLM-over-ffmpeg, ~4/6 stages, but NO creative grade + clip-by-clip not single-file). Also `isaacrowntree/color-grade-ai` (MIT, LLM→.cube pattern), LumiVideo (arXiv 2604.02409, decision-layer design, no code). Verdict: **FORK-AND-EXTEND** (study perbhat's loop, add the two missing pieces), don't build-from-zero, don't wrap a closed GUI (the good ones have no API). Novelty = packaging, not concept — don't overclaim externally. Lesson: "does this exist?" deserves an adversarial-verify pass — the honest answer ("idea mainstream, this exact combination empty") is more useful than yes/no and de-risks the build.

- **BUILT + VERIFIED the engine same day (autonomous run while Elijah was at his birthday dinner):** the full pipeline works end-to-end on his REAL footage — stills, single-clip, AND multi-scene video (PySceneDetect found 3 shots → 3 DISTINCT per-shot constant corrections [anti-flicker] → Stage-2 shot-match to a hero → uniform creative LUT → concat). All 5 film LUTs render correctly (verified visually, no channel swap). The adversarial-review workflow caught + fixed a CRITICAL R/B LUT-write-order bug, a gamma-sign bug, and a WB still/video-parity bug BEFORE first run. A runtime bug surfaced in testing: ffmpeg `lut3d` rejects absolute Windows paths (drive-colon + spaces) under every escaping/quoting we tried → fixed by running ffmpeg with `cwd`=cube-dir and bare filenames (io_utils.run_ffmpeg gained a cwd param; color.py stages all cubes into one dir). cv2/colour/skimage import in 0.8s in the auto-clip/.venv (the OneDrive cv2-hang gotcha did NOT bite a clean venv). Engine = `auto-clip/color.py` + `colorkit/`; DRAFT (uncommitted, stops at `out/`). Lesson: the "nobody ships this exact combo" gap was real AND buildable in ONE session — fork-study the closest blueprint (perbhat) + assemble mature permissive bricks (OpenCV/ffmpeg/PySceneDetect/colour-science), and let an adversarial review workflow catch the math bugs before you ever run it.

## 2026-06-23 — "Stop Vibe Coding" carousel built (editorial / liquid-glass, hero-image-per-slide)

- **Third tworkflow carousel concept BUILT** — the "Stop Vibe Coding" angle in a NEW look (Editorial Manifesto: cream paper · bold black grotesque · signal-red accents · liquid-glass text panel · distinct concept image per slide). Files in `carousel-builds/tworkflow-vibe/` (8× 1080×1350 + contact sheet). Vault: `Stop Vibe Coding - tworkflow Carousel.md`. DRAFT, unposted.
- **What Elijah's eye demands (reuse on future carousels):** (1) every slide needs an ACTUAL different picture/concept — he rejected "same background recolored" instantly; build a per-slide concept-image system, not one shared backdrop. (2) tag `@elijahaifl` in the top-left CORNER on the bg, NOT inside the graphic/panel. (3) carousel text belongs in the vertical MIDDLE (bottom gets clipped by IG UI), on a **liquid-glass** panel — translucent/see-through, not opaque frosted. (4) lock the look on ONE cover over a few iterations, THEN batch.
- **Production method that worked for a varied-imagery set:** batch all 8 in parallel with an IDENTICAL style preamble + a unique concept-image + copy per slide, and NO shared image reference (a reference bleeds the cover's subject — e.g. the tower — into every slide). GPT Image 2 held style consistency from the prompt alone and rendered all text clean incl. a 7-node loop diagram. Crop top-biased (`crop=1744:2180:0:0`) to keep the corner tag. ~77 credits incl. cover iterations.

## 2026-06-21 — second carousel for the SAME repo, deliberately different frame (tworkflow)

- **Repurposed the proven "WORK" reel (114 comments) into a carousel advertising `clarity-digital-development/tworkflow`** — but framed as the OPPOSITE angle to the existing [[Claude Runs Your Instagram - AI Dashboard Carousel]]: that one = the IG-dashboard repo / "grow your IG"; this one = the build-*method* repo / **"AI agents lie to you — here's the free system that catches them"** (process-beats-model, anti-slop). Draft saved to `20-Content/Ideas/AI Agents Lie To You - tworkflow Carousel.md`. Reused the **WORK** keyword so it rides the now-fixed comment→DM automation + same repo delivery. Lesson: when re-advertising one asset, change the PROMISE (different outcome) not just the words — two carousels can point at the same repo without cannibalizing if the frames don't overlap.
- **tworkflow's real, ad-able substance (from its README, don't overclaim beyond this):** 7-step loop Context→Plan→Implement→Review→QA→Ship→Retro; hero insight = the **40% context rule** (reset before degradation); catches hallucinated deps / false "done" / sycophantic review / context rot; ships 9 guides + 6 templates + 2 Claude Code skills + a worked example; MIT, model-agnostic. Attribution caveat flagged for Elijah: repo is Clarity Digital ([[Tanner]]) — confirm "I" vs "we" framing before posting.
- **BUILT same day** — all 8 slides rendered Terminal Noir, reference-driven off an approved slide-1 hook (GPT Image 2 image-to-image kept the set identical). Method that worked: lock layout + style on ONE hook slide first (2 iters), THEN batch the other 7 in parallel off it as the `medias` image reference → cohesive set in one pass, ~56 credits (8×7 at high/2k). GPT Image 2 rendered every list/loop/number cleanly. Crop 3:4→1080×1350 via ffmpeg `crop=1744:2180:0:0,scale=1080:1350` (top-biased to keep the handle). Files in `carousel-builds/tworkflow/`. DRAFT — unposted, pending Elijah's per-post confirm.

## 2026-06-21 — comment→DM automation outage + the manual-recovery playbook

- **Elijah's comment→DM tool ("mini chat" = ManyChat-class) silently died → a whole giveaway's worth of leads got nothing**, his own from-another-account test included. Total blackout across ALL flows (not one broken flow) = the break is at the **IG↔tool connection/permission layer**, not the automation config. Top causes to check IN THIS ORDER next time: (1) IG app toggle **Settings → Messages and story replies → Message controls → Connected tools → "Allow access to messages"** flips OFF on its own (app updates / re-login) and kills every tool silently — check this FIRST, 30-sec fix; (2) connection expired → tool's **Refresh Permissions** / reconnect; (3) 2026 IG API cap is **200 calls/hr** (down from 5,000) — a comment surge can blow it and pause sends ~1hr; (4) account flipped to Personal; (5) known Meta-side bug → tool support. The IG account itself was healthy the whole time (MCP profile pull worked) — confirms it's the bridge, not a ban.
- **Tools CANNOT "go back" and send to missed comments** — when the connection is down Meta DROPS those comment webhooks and never replays them; there is no backfill button. BUT the **comment private-reply window is 7 days**, so missed commenters stay reachable for a week. We recovered the "WORK"→`tworkflow` repo reel ([reel DZymqsHxJsk], ~24h old): replied the repo link to all 12 accessible keyword commenters.
- **HUB MECHANIC (reuse this):** the `instagram` MCP can recover via **`reply_to_comment`** (public, 2200 char, needs `comment_id` — which `get_comments` returns) but **NOT via `send_dm`** — `get_comments` returns usernames only, NO IGSID, and `send_dm` needs IGSID + is capped to the 24h window. So **public-reply is the only API-executable recovery path**; it's fine when the deliverable is a public link (open-source repo). Dodge IG spam-flagging by **varying the wording** across replies (sent 12 distinct phrasings, all landed). Coverage caveat: `get_comments` surfaces ~top-level only (26 of 114 here; rest are nested replies the API won't page cleanly) — so a "work" typed as a *reply under* another comment can be missed.
- **Prevention worth proposing (Elijah-gated):** a connection-health monitor (alert when the comment automation goes quiet) + a public auto-reply on keyword comments as a delivery backstop, since for a public-link giveaway a reply delivers as well as a DM.

## 2026-06-20 — Instagram "Edits app boost" investigated: real but small + temporary; the durable win is quality/audio

- **Elijah's belief is PARTLY TRUE — an explicit Edits reach boost WAS Meta-confirmed, not folklore.** Mosseri (Aug 2025): "Edits does help a little bit with reach" via a real ranking signal (predicting Edits-button taps); IG VP Westervelt reconfirmed Nov 2025. BUT his own words: "a little bit" + "not going to be around forever" (an app-adoption bootstrap), the 2026 record flipped to editing-tool NEUTRALITY (no 2026 source confirms it's still active), and the one direct A/B test = ~0.4% view diff. Real, bounded, fading — not a durable structural shove. Don't repeat my earlier "it's a flat myth" overclaim.
- **The durable, legit wins (persist past the promo boost):** sharing direct from Edits = "priority processing" + higher export quality (Meta's own Edits PM) → protects watch-time (the #1 signal); watermark-free (avoids the one real editing penalty); reads as original. **HUB-SPECIFIC + actionable:** the `instagram` MCP API-publish path CANNOT use licensed/trending audio (standing rule §4) → for audio-driven reels the Edits/in-app path is genuinely better for Elijah (only way to get trending sounds). Implication: keep using Edits, but for quality/audio/originality — not a secret multiplier. Real levers stay hook (first 3s) · watch-time · sends-per-reach (~3–5× a like).
- **Pending data test (Elijah-gated):** split his reels Edits-vs-native, compare on 2026 signals via `metrics2026.py` + `watchtime_ideator.py` — causation caveat: cohorts differ on hook/audio/recency (and API reels can't use trending audio), so a win ≠ an "Edits flag." Needs him to tag which reels were Edits-made.
- **Context/method:** triggered by his new **Insta360 Luna Ultra** camera → settings/offload/edit Q&A (specs+workflow saved to auto-memory `reference-insta360-luna-ultra`). 5-angle adversarial workflow (steelman-boost + official + indirect + 2026-changes + real-levers, each verified), weighting Meta-official/A-B evidence above blog claims.

## 2026-06-19 — carousel production method that WORKS (reference-driven GPT Image 2) + OBS/Apollo fix

- **The "Claude Runs Your Instagram" 8-slide carousel is BUILT** — files in `carousel-builds/claude-runs-ig/slides/slide1-8.png` + `contact_sheet.png`. Cinematic dark/orange "phone erupting with content" style, hook = "Claude can now run your Instagram better than you / the free open-source method."
- **Reusable carousel METHOD (this is the win):** generate ONE master cinematic hook via Higgsfield `gpt_image_2` image-to-image (off a base render), then pass THAT job id as the `--image`/`medias` reference for every other slide with prompt "keep this exact style, replace ALL text with: [headline + accent words + subtext]." Result: a cohesive set, and GPT Image 2 renders integrated headline text CLEANLY — long copy, lists, AND exact numbers ("5M", "1,378", "50+", "1,378" with comma) all correct. No flat HTML overlay needed (Elijah rejected that look). Earlier mistakes to NOT repeat: don't bake the IG UI chrome (@handle, "1/8" page counter) into the art — those were just Instagram's app UI in reference screenshots.
- **Higgsfield MCP flow (server `e46b6dc5…`):** `generate_image` returns a pending job id (async ~90-150s at 2k/high) → `show_generations` to get rawUrl → `curl` download → `Read` to view. Don't poll-spam; background-sleep ~95s then fetch. Assembly/contact-sheet = headless Edge (`msedge --headless=new --screenshot --window-size`) rendering local HTML that references the PNGs.
- **OBS + UA Apollo (Elijah solved it):** OBS has NO native ASIO; the Apollo is ASIO-first, so the Windows WASAPI endpoint feeds OBS a dead/garbage (static, non-responsive) stream even though monitoring/speakers work fine (monitoring is a separate path). Fix = the **obs-asio** plugin (GitHub). Reboot also clears a glitched USB record stream after an OBS crash. Vocal chain: record clean, process in post (his ffmpeg chain), keep Apollo input light; mute the input channel in UA Console to kill speaker bleed without affecting capture.
- ✅ **RESOLVED — the carousel's payoff repo is BUILT** (2026-06-20): standalone open-source repo git-committed at `C:\Users\elija\OneDrive\Desktop\ig-claude-dashboard` (generic, MIT, no personal data — data/ gitignored). Contents: generalized `refresh.py` (account id from env, urllib→curl auto-fallback for the OneDrive-host urllib stall, stdlib-only), `dashboard.html` + Chart.js, `setup.py` (auto-discovers INSTAGRAM_ACCOUNT_ID from a token), `CLAUDE.md`, `.mcp.json` (instagram MCP), and two skills — **connect-instagram** (the full token-setup walkthrough — the hard part) and **plan-my-content** (2026-signal analysis). Verified working end-to-end via `setup.py` against the live token. NEXT (Elijah's call): push to GitHub as `elijahaifl/ig-claude-dashboard` (public) → then the carousel (slides 6+8) has a real repo to deliver and can post.

## 2026-06-18 — long-form video pivot: scripts built + 6-month data pulled

- Elijah shifted to long-form YouTube (shoots raw off bullets — method works; he filmed the "5 Secrets to Success" 30-min in one go). Built vault script notes in `20-Content/Scripts/`: the **age-14 testimony** (FLAGSHIP — the devil's soul-bargain he refused; the deception was his self-serving lifestyle, NOT his accomplishments; sensitive: drugs/police-tasing + a self-harm disclosure → detail level is his call), **"backwards" business video** (5 counter-advice truths: leverage>cash · reps>idea · reputation=capital · kill dead ideas fast · partner 50/50), and **"How I Grew to 100k — the Data, Not Vibes."** Raw answers in `20-Content/Ideas/Longform — Real Answers & Talking Points (2026-06-18)`.
- **Unique AI thesis to reuse:** "build software FOR agents, not people" + the hard line "all-in on AI, but I won't put it in my body" (Neuralink = mark of the beast — from the testimony).
- **6-month data (pulled via `ig-dashboard/sixmonth_pull.py`, 2026-06-18):** 675 posts/~6mo (~3.6/day), 101,128 followers, 55.4M+ views (only 55% of posts measured → real total likely ~80M+). Breakouts: 10.0M AI/Tech reel (Feb-24), 5.6M Money reel (215k shares). Niche-by-views: AI/Tech 19.8M › Money 14.2M › Motivation 6.6M (Motivation = MOST-posted at 168 but low return). **672/675 = reels → carousels totally untapped** (confirms the carousel build is real upside). Engagement is SHARE-driven (top posts 100k–215k shares). No 6-mo follower curve available (Meta snapshots forward only; account daily ~30d cap).
- **Personal:** first child due ~Aug 2026.

## 2026-06-17 — IG carousel "monetization": the 5M figure is a GATE, not a payout

- **What Elijah got "eligible" for is the invite-only *seasonal bonus for photo & carousel posts*** (US/Japan/SK test under the Bonuses/Content-Monetization umbrella), NOT an always-on payout. The "5M views/mo × 3 consecutive months" he saw is a **one-time eligibility/qualification GATE to be *invited*** (region-tiered: JP 500K / SK 1M / US 5M) — it is not a payout floor or per-post tier. His REELS reach cleared it despite ~zero carousels. **Eligible ≠ invited ≠ earning**: he still needs the invitation banner in Professional Dashboard → Activate bonus → set up payout/W-9. Source: Meta `help.instagram.com/708013994693013` (© 2026), verified 2026-06-17. Full report: `docs/plans/2026-06-17-ig-carousel-monetization.md`.
- **What it really pays (honest):** no fixed rate; ~**$0.09–$0.50 per 1,000 *qualified* views** (~$90–$500/M), finance/AI at the high end, per-deal **capped**. Verified-FALSE numbers to never repeat: $10–$50/1k (that's TikTok, off by 100–1000×); "Reels Play Bonus 2.0 $0.12" (dead program, shut Mar 2023). Math: 5M ≈ a few hundred–$1.5k/mo; 10M ≈ ~$1–3k/mo. **Implication: the bonus is gravy — the real prize is carousels as an untapped reach/saves/funnel format. Optimize for saves/sends + funnel to ventures, not the RPM.**
- **Method note:** focused workflow (4 finders → adversarial verify 12 volatile claims → synth) caught 4 outdated $-figures the sweep believed; one workflow at a time held, no throttle. Confirms the research protocol — verify the volatile $/eligibility facts, don't trust the first source on fast-changing program terms.
- **Live Graph-API field probe (read-only, raw curl via refresh.py) beats the MCP for analytics scoping.** The connected `instagram` MCP is a thin wrapper with a HARDCODED metric allowlist (media: reach/likes/comments/shares/saved/video_views; account: reach/profile_views/website_clicks/accounts_engaged) — it CANNOT request advanced fields; refresh.py already surpasses it via raw Graph API. Probe verdict for what's pullable-but-uncaptured: ✅ reach/views split by content type (`breakdown=media_product_type` — live: reels 5.0M reach vs carousels 1,378/30d), ✅ stories insights (he had 8 live stories), ✅ reached/engaged-audience demographics (needs `timeframe=this_month`/`this_week`; older rolling values deprecated — live: US 2.43M/GB 763k/CA 375k this month), ✅ account daily follows_and_unfollows. ❌ KILLED: per-post `profile_visits`/`follows`/`profile_activity` — Media Insights API no longer supports them (can't attribute follows to a specific reel). Implication: a propose-only refresh.py v3 should add content-type split + stories + reached-demographics; don't promise per-post follow attribution.
- **Content idea drafted (DRAFT-only, vault):** "Claude runs your Instagram + open-source AI dashboard" carousel ([[Claude Runs Your Instagram - AI Dashboard Carousel]]) — comment→DM a FREE standalone repo of JUST the ig-dashboard (not the hub/abc-wrap). Blocks on actually building+scrubbing that standalone repo before posting (hardcoded ACCOUNT_ID→env, strip his data). Frame guardrail: don't claim "IG just dropped their API" (false).

## 2026-06-15 — first level-up pass: the bottleneck is activation, not model quality

- **The hub's biggest gap isn't a missing tool — it's that 7 validated engines never run automatically.** First-ever level-up pass (`docs/plans/2026-06-15-level-up.md`) found the scheduled task now runs only the 3 *original* engines via `Daily Agent Refresh.bat`; metrics2026, watchtime_ideator, viral-radar, news-radar, viral_teardown, trial_ab, and grade.py fire only on manual skill calls → stale outputs. The grade loop proves the model is good (held-out Spearman +0.455–0.527), so the highest-leverage move is a PROPOSED `Weekly Agent Refresh.bat` + Sunday task, not buying a 31st intel source. Implication: a built engine isn't "done" until it's scheduled OR documented as on-demand-only — close the measure/generate seam at the Ship gate.
- **Three frontier themes worth acting on, all cheap-slice:** (1) IG 2026 ranking = watch-time · **sends-per-reach** · likes-per-reach, sends 3–5× a like — audit whether metrics2026's `share` maps to IG's send/reshare field (NEEDS API field verification — don't promise a `sends` breakdown until confirmed exposed). (2) Episodic open-loop **mini-series** ("Part 1→N") is the dominant 2026 breakout format (3× save rate) — the hub has only single-asset generators; a `series-planner` skill is the net-new slice. (3) ACE (ICLR 2026) flags this very memory.md's append-only regime for brevity-bias + context-collapse — run the already-installed-but-unused `consolidate-memory` skill quarterly as a Curator pass.

## 2026-06-15 — self-improve loop closed: the hub now grades its own model

- **The 2026 score is validated out-of-sample — it actually predicts reality.** New read-only loop `self-improve/grade.py` graded `metrics2026` on a held-out split (train older 70% / test newer 30%): Spearman **+0.455 vs realized views**, **+0.476 vs avg watch-time** (n=90 it never saw), and the skip-gate is valid (healthy 14.8k vs throttled 5.4k mean views). Implication: rank on metrics2026 with confidence — and the genuinely-new frontier is now INSIDE the loop, so close self-improve L2 (log Higgsfield's pre-publish hook score so it can be graded vs realized watch-time, the 5–10× lever post-hoc rates can't see) rather than buying a 31st intel source. Propose-never-apply held all night: the grader writes only its append-only ledger, and the +0.023 weight-tune stays a PROPOSAL — harden it against fooling itself (majority-of-k-splits) before applying.

## 2026-06-14 — overnight autonomous research+build program (6 builds + 30-candidate research)

- **Autonomous overnight loops run throttle-free at ≤5 concurrent agents, ONE workflow/agent at a time.** Evidence: 6 research waves (~7 agents each) + 6 build subagents ran clean over hours, zero throttling. Implication: pace via background-task completions (not bursts); keep the orchestrator's context lean by delegating + writing all state to disk (the tracker survives compaction).
- **The hub was scoring on the WRONG signal.** Built `ig-dashboard/metrics2026.py` — the 2026 IG metric contract (skip>share>like>save>comment rates, skip-gated, graded vs his own 300-post distribution). `watchtime_ideator`, `intel/viral-radar`, and the `content-intel-2026` skill all consume it. Implication: rank on rates, not raw likes/views.
- **Data finding (act on it):** Motivation/Life is his MOST-posted niche (94×) but LOWEST watch-time hold (8.3s); AI/Tech holds best (17.5s); Money/Finance is a skip-rate risk (rides reach, not depth). Implication: rebalance toward AI/Tech + lean-in arbitrage (Child Safety/Faith), tighten Money/Finance retention.
- **Subagents excel at safe ADDITIVE builds** (stdlib/curl, read-only, test, local-commit) — 6/6 shipped clean. Implication: delegate builds to save orchestrator context; keep them additive (never edit production engines unsupervised).
- **Security (rule-3 violation):** sibling `..\abc wrap\.mcp.json` has a hardcoded plaintext `GEMINI_API_KEY` — rotate + move to env var.

## 2026-06-13 — auto-clip "brain" completed: Claude IS the highlight selector

- **The missing "brain" is solved without any new dependency:** `highlight.py` gained a `--provider agent`
  path where the Claude Code agent makes the editorial clip selection (two-step: emit a brief → agent writes
  `picks.json` → Python does the deterministic seg→time mapping/sort). No `ANTHROPIC_API_KEY`, no Ollama (not
  installed), no install. Evidence: ran transcribe → agent-highlight → reframe end-to-end on `source.mp4` →
  6/6 valid 1080×1920 H.264/AAC clips. Implication: auto-clip is **usable interactively today** via the new
  `auto-clip` skill; the anthropic/ollama providers are only for unattended/scheduled runs.
- **Pattern worth reusing:** for a "local agent hub," the cheapest, highest-quality LLM step is often the agent
  itself — wire a `--provider agent` mode instead of requiring a paid key or a multi-GB local model.
- **Research keys (Exa/Tavily/Firecrawl) 401 is NOT a bad-key problem:** they're valid at User scope (len
  36/57/35) but absent from the Claude *process* env (INSTAGRAM token IS present). The MCP servers spawned with
  empty keys. Fix = a true full quit + relaunch of the desktop app (not a window close). Do NOT re-issue them.

## 2026-06-13 — business-ops + productivity research (round 3) + a throttle lesson

- **Scheduling:** Metricool stays the cross-platform pick — but its MCP needs the **Advanced ~$54/mo** plan (blueprint said "any plan" — stale), and IG licensed-audio auto-publish is now **partially** unlocked (needs Facebook-Login auth; trending-sound catalog may be narrow). TikTok still MUST go through an audited client (never a self-built MCP). Full report: `docs/plans/2026-06-13-business-ops-productivity-research.md`.
- **Course platform:** migrate Archetype Index → **Whop** (verified: official `@whop/mcp` + native Courses + lowest fees, no monthly). Skool only if it's community-first. Tie-breaker O3 in the report.
- **Payments/CRM/newsletter:** **Stripe official MCP** (restricted key in env var — NOT Composio's cloud vault); sponsor CRM = a **vault `sponsors/` folder**, no SaaS; **beehiiv** MCP is read-only V1 + Send API is Enterprise-beta (later/at-launch).
- **Productivity:** nothing new to buy — reuse `auto-clip/transcribe.py` + the vault + the 7AM scheduled task for podcast→notes + a daily briefing.
- **THROTTLE LESSON:** even a single 5-agent workflow partial-throttled (3/4 research agents rate-limited) when Anthropic was load-shedding. Recovered per protocol: did NOT relaunch the fan-out; spot-verified the load-bearing claims with direct WebSearch instead. Reinforces: keep research fan-outs small, and verify-don't-relaunch on a throttle.

## 2026-06-13 — capability research round 2 (audience-sim / Composio / Playwright)

- **Pre-publish audience simulation is viable & novel:** camel-ai/OASIS (Apache-2.0, local) is the pick — a QUALITATIVE hook-ranker seeded from his audience that PAIRS with Higgsfield (OASIS gates the hook, Higgsfield gates the edit). Caveat: simulates X/Reddit not IG, ~30% RMSE → must backtest vs the 300-post history before trusting. PLAN: `docs/plans/2026-06-13-audience-sim-pipeline.md`.
- **BettaFish/MiroFish = not for us:** China-platform public-opinion analysis, restrictive licenses (GPL/AGPL), overlap owned tools. Cherry-pick only BettaFish's multi-agent "debate" pattern.
- **Composio = add-later, scoped:** powerful 500+ toolkit MCP gateway, but its cloud vault stores tokens OFF-machine → conflicts with the secrets-local rule. OK for low-stakes Notion/Calendar/Sheets; NEVER route IG/Meta tokens through it. Self-host is Enterprise-only.
- **Playwright MCP = skip now:** overlaps the already-wired Claude-in-Chrome + Firecrawl; only wedge is headless unattended scheduled browser jobs (not a need). Logged-in IG/TikTok browser scraping = ToS/ban — public pages only.

## 2026-06-13 — capabilities recon + research (Workflow 1)

- **Auto-clipping is ~70% already built, not a from-scratch project.** Local transcription (faster-whisper 1.2.1 installed), the editorial cut-list brain (`abc wrap/interview/work/build_cutlist.js`), and a data-driven crop comp (`abc wrap/src/edits/PodcastEdit.tsx`) all exist. Only 3 new pieces needed: local-transcribe JSON wrapper, an LLM highlight/moment **selector** (the true missing brain), and face-track 9:16 reframe. Implication: build small + reuse; don't rebuild captions/b-roll/export.
- **GPU is idle:** RTX 5070 (Blackwell sm_120) present but torch is the +cpu build → faster-whisper runs CPU-only. Needs the `cu128` torch wheels + cuDNN9. Highest-leverage perf fix before any clipping work.
- **Carousels are pure untested upside:** he runs zero today. Optimize a carousel skill for SAVES first (his live save-rate peaks ~3.6% on AI/Tech utility); Money/Finance remains the view-leader (~25× Motivation).
- **Competitor intel = two layers:** free `business_discovery` (own token, zero ban risk, counts-not-text) + Apify pay-per-result for comment TEXT/TikTok/YouTube (never touches his token). Bright Data is enterprise-overkill ($250 min). Do sentiment in-house with Claude, not a paid actor.
- **DMs at scale need Meta App Review (Advanced Access).** Error #3 = Standard Access only. Comments (read/reply/hide) work today; DM replies only inside the 24h reactive window; cap ~200 business-initiated msgs/hr. HUMAN_AGENT tag must never be auto-applied (revocation risk). Triage must be webhook-driven + draft→human-approve→send.
- **Env-var landmine recurred:** research keys (Exa/Tavily/Firecrawl) were set correctly at User level but absent from the running process → 401. Only a FULL Claude restart loads new env vars; `/clear` keeps the same process. Verify MCP keys with one live call after any session that added env vars.

## 2026-06-12 — seeded at system creation

- **Category arbitrage detected:** last 30d — Money/Finance: 4.15M views from 20 posts (~207k/post) vs Motivation/Life: 554k views from 66 posts (~8.4k/post). Implication: shift volume from Motivation toward Money/Finance and AI/Tech until the data says otherwise.
- **Publishing quota is a non-issue:** live check shows 0/100 daily API publishes used. The real IG API constraint is no licensed/trending audio on API-published reels.
- **TikTok cannot be self-automated** (unaudited apps post private-only). Route TikTok through an audited scheduler (Metricool) or skip.
- **Infrastructure:** IG token auto-renews its expiry display (real expiry 2026-08-11); dashboard self-feeds stats.md daily at 7:00 AM.
