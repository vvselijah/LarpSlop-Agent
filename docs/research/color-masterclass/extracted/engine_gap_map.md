# Colorkit Engine Gap Map

_Generated 2026-06-26 from `engine_gap_map.json`. **18 techniques** mapped to the colorkit engine; **6 claims flagged** for human re-check._

**By priority:** P0: 3 · P1: 5 · P2: 8 · P3: 2

**By status:** missing: 11 · partial: 7

> Derived from frame-by-frame analysis of 14 named-colorist masterclasses (see `synthesis_v2.md`). Each row: demonstrated technique → the colorkit module it touches → the concrete engine change → cited evidence. A merge bookkeeping row tracks 10 further videos acquired 2026-06-26 whose vision analysis is still pending.

## P0  (3)

- **[missing] Skin-tone-line solver: mask skin ROI, measure hue angle vs the ~123 deg I-line + skin chroma, then a small CAPPED rotation toward the line (soft, never a hard snap; allow a slightly-green filmic resting offset).** (`measure.py`) — Add measure.skin_signature(bgr) -> {skin_mask_frac, skin_L_IRE, skin_hue_deg, skin_chroma} using the existing YCrCb skin rule (Cr 133-173, Cb 77-127) tightened with a luma gate; compute hue via atan2(Lab b*, a*). Add a capped skin-hue/balance stage in correct.compute_correction that nudges WB/tint toward putting the skin angle on (or a tunable few deg under) the I-line, gated so it only fires when skin_frac >= ~0.08 and damped to a small max delta.  _ev: cullen-contrast 11:40 (judge by eye, reject hard snap); mostyn-read-scopes 00:42/05:37-05:42/04:55 (on/under/over OK if consistent, film trends slightly green); grading-too-complicated 07:20-07:22 (yellow=on-line, magenta=overshot); 2hr-beginner-course 21:38 (HSL skin key hue~32, luma 54-63)_
- **[missing] Two-band skin EXPOSURE gate measured on the skin ROI in IRE: midtone bulk 40-50 IRE, highlights 55-65 IRE, hard ceiling ~70 IRE; flag over-exposed skin; measure the ROI not the whole frame.** (`measure.py`) — In measure.skin_signature compute the skin-ROI luma percentiles in IRE. In correct, after the global median->0.18 exposure, apply a small skin-gated exposure trim toward the 40-65 IRE band ONLY on the skin region's contribution; emit a 'skin over 70 IRE' / 'skin under 40 IRE' warning into the log.  _ev: mostyn-read-scopes 02:21/02:40/04:35/05:21 (40-50 mid, 55-65 hi, ~70 ceiling); 04:22-04:31 (measure skin ROI, bright bg fools the eye)_
- **[missing] Headless scopes as self-validation: RGB parade per-channel histograms, luma waveform summary, 2D vectorscope hue-mass/centroid, clip-percentage at 0 and 1023, on a 0-1023 scale. Trust them as ground truth.** (`new scopes.py`) — New scopes.py (numpy-only): parade(rgb)->per-channel histograms+low/high percentiles; waveform(L)->occupancy; vectorscope(rgb)->2D hue-mass + centroid angle/radius; clip_stats->pct at 0 and at 1023. color.py computes scopes on each shot's corrected rep frame, logs the targets, and writes an optional --validate JSON sidecar.  _ev: mostyn-read-scopes 00:24 (software scopes = ground truth) + 0-1023 throughout; beginners-guide 12:41 ('scopes cannot be deceived'); cullen-master-scopes 08:25; 2hr-beginner-course 10:55 (clip = lost information)_

## P1  (5)

- **[missing] Assert the demonstrated scope targets after develop: black floor off 0, diffuse white ~896/1023 (~87.5%), no channel pinned at 1023, parade R/G/B converged toward neutral, skin lobe near the I-line.** (`new scopes.py`) — Add scopes.validate(rep_after_develop, skin_sig) returning pass/fail per target + a one-line verdict; surface in color.py's per-shot log and the --validate sidecar. Non-fatal (advisory), but enables future auto-tuning.  _ev: beginners-guide 07:09/16:15 (white ~896, black off 0); best-node-tree 13:04-13:32 (balanced parade, skin on I-line, no clip); film-emulation 16:15_
- **[missing] Halation = linear-light, red-weighted highlight bloom: linearize -> Gaussian blur+glow on the top ~20% luma (flat below a high knee) -> recombine with Add/Screen; intensity scalar (default ~0.5).** (`stylize.py`) — Add stylize.halation(rgb_linear, knee~0.8, strength~0.5, red_weight) operating in linear (tonemap.srgb_eotf/oetf already available): build a high-luma mask, Gaussian-blur a red-weighted copy, Screen/Add composite. Wire as an optional toggle on a look preset (the look becomes an ordered stack, not one cube).  _ev: film-emulation-16-35mm 11:36-12:26/17:56 (linear CST sandwich, top-20% luma mask, Add); mostyn-perfect-node-tree 40:19-40:24 (Glow threshold 0.75, Screen beats Overlay)_
- **[missing] Film grain = monochrome (sat 0) Overlay noise, mid/shadow-weighted (clean highlights), size ~0.36 / strength ~0.38; pre-render a tiled plate ONCE (heaviest op) and composite at low opacity.** (`stylize.py`) — Add stylize.grain: generate/cache a tiled monochrome noise plate; composite Overlay at low opacity with luma weighting (shadows>mids>>highlights). Pre-bake the plate to avoid per-frame regen; expose size/strength/weighting. Toggle on look presets.  _ev: 2hr-beginner-course 62:14-62:58 (size 0.357/strength 0.380, mid-weighted, monochrome Overlay); mullins-grading-philosophy 54:44 (Shadows 0.605 > Mids 0.287 > Highlights 0.000)_
- **[partial] Constrained white balance: keep Shades-of-Gray estimate but DAMP any WB move that worsens the skin-hue angle (maximize separation s.t. skin no-worse); add optional per-zone (shadow/highlight) neutralization.** (`correct.py`) — In compute_correction, after WB estimate, recompute the predicted skin-hue angle and damp wb_gains toward neutral if the move pushes skin off the I-line. Optionally add a windowed (bottom-percentile) per-channel black neutralization separate from the highlight balance.  _ev: cullen-contrast 09:57-11:30 (Two Keys: separation + skin no-worse); mostyn-read-scopes 08:58-09:11 (windowed black-guide, drive R=G=B at the floor); beginners-guide 17:57-18:55 (per-channel parade convergence)_
- **[missing] MERGE BOOKKEEPING (2026-06-26): 10 additional color-grading masterclasses are acquired + dense-frame-extracted + transcript-batched but NOT yet vision-analyzed; no demonstrated parameters extracted yet, so no engine change is justified until handoff step 4 runs over them.** (`(research pipeline / no engine module yet)`) — NONE yet. Action: run the color-masterclass-deep vision workflow (handoff step 4) over slugs batal-conquer-color-page, benkabouche-1h-master, content-creators-indepth, euro-pro-level-64min, faris-full-course-2026, jenkinson-exact-system, lenz-most-important-concept, neistadt-ultimate-guide, qazi-start-here, wampus-pro-seminar; then re-run the merge so any newly demonstrated params (esp. G3 film texture + G4 CST/wide-gamut) update or add rows.  _ev: raw/batches_index.json (10 slugs, 354 total batches / ~6919 frames acquired 2026-06-26); RESEARCH-HANDOFF.md step 4 (vision pass pending); editing-explained-2hr downloaded but not dense-extracted (no batches)_

## P2  (8)

- **[missing] Vignette = feathered elliptical radial luma falloff darkening edges to lift the subject; store size/aspect/pan/tilt/softness.** (`stylize.py`) — Add stylize.vignette(rgb, size, aspect, softness, strength) as a radial luma multiplier (pixel path) plus an ffmpeg 'vignette' filter string for the video path; toggle on look presets. Keep it subtle/feathered.  _ev: frenchie-masterclass 20:49 (Size 52.96/Aspect 78.03/Soft1 18.0); 2hr-beginner-course 42:08-42:52 (circular PW, single-digit lift to avoid halo)_
- **[partial] Look-time saturation as density/subtractive (deepen, not electrify) or a luma-preserving 3x3 matrix, with a protected Skin band — NOT a naive HSV/per-channel multiply.** (`luts.py`) — Replace luts._saturate with a density/per-hue model: scale chroma sublinearly (deepen) and exempt a Skin hue band; or apply a luma-preserving channel-mix. Keep develop vibrance as-is.  _ev: frenchie-masterclass 22:28-22:33 (subtractive density deepens not electrifies, +14%); grading-too-complicated 09:38-11:51 (HSV/per-hue density, never RGB gain); mullins 32:12 (luma-preserving 3x3)_
- **[partial] Expose contrast amount AND a linear-pivot bias as tunable params (low pivot protects highlights, high pivot protects shadows); demonstrated working contrast is scene-adaptive 1.0-1.10 for clean footage.** (`correct.py`) — Thread a 'contrast' and 'pivot_bias' through compute_correction/develop_fn (default 1.08 / 0.18 so the approved look is byte-identical); let decide.py or a flag set them per scene. KEEP linear pivot at 0.18 — do NOT change it to 0.435 (that constant is for log/encoded space, which colorkit never grades in).  _ev: mullins 21:05-21:52 (0.435 = 18% grey in log, NOT 0.5) + 20:34/20:41 (pivot biases protection); grading-too-complicated 08:26 (pivot is working-space-dependent); qualifier-tricks 02:09 (contrast 1.104)_
- **[partial] Optional filmic develop curve: lifted/soft toe + rolled shoulder (raised black, no white clip) — the demonstrated film-print tonality, a single S/CST response.** (`tonemap.py`) — Add an optional lifted-toe term to the develop tone curve (small black lift + soft toe) in tonemap, exposed as a develop param defaulting to off, so a 'filmic' develop variant can raise the toe like a print stock without re-stacking the look LUT.  _ev: mullins 43:45 (single CST -> soft S, lifted toe + rolled shoulder); film-emulation-16-35mm 16:15 (milky toe ~80-120, rolled shoulder); 2hr-beginner-course 32:12 (gentle S, soft-clip knees)_
- **[partial] Shot-match metric = maximize 2D vectorscope hue-MASS overlap (distribution), skin-weighted and excluding saturated set-dressing — stronger than per-channel LAB mean/std.** (`match.py`) — Add a skin-weighted / set-dressing-masked variant of compute_match_params (down-weight high-chroma non-skin pixels) and/or a hue-mass-overlap score to pick the hero and validate the match. Keep the constant-per-shot baked-LUT mechanism.  _ev: cullen-master-scopes 08:52-11:07 (hue-mass overlap = match metric) + 07:44 (saturated set-dressing is a trap, judge on skin lobe); cullen-36 25:50_
- **[missing] Temporal averaging: accumulate the scope/measurement read over several frames before committing a correction (reduces single-frame noise).** (`measure.py`) — Optionally sample N rep frames across the shot, average the scene_stats / scope reads (and skin signature), then derive ONE constant correction from the averaged stats — preserving the constant-per-shot anti-flicker rule.  _ev: cullen-master-scopes 08:18 ('show it to me enough times'); mostyn-read-scopes (temporally build the diagnosis)_
- **[missing] False-color / IRE band map (luma -> -5..+5 bands, band 0 ~= 0.391 mid-grey) for a per-pixel/per-region exposure-zone check before grading.** (`new scopes.py`) — Add scopes.false_color(L) bucketizing Rec.709 luma into IRE bands; use it to drive the skin-exposure gate and emit a 'subject under / background hot' note. Optional debug PNG.  _ev: mostyn-read-scopes 00:46/02:09 (permanent false-color IRE legend); grading-too-complicated 05:36-05:37 (band 0 = mid-grey 0.391, -5..+5)_
- **[missing] Model each look as an ordered, toggle-able STACK (color cube + halation + grain + vignette + density), not a single opaque tint cube — the demonstrated film-emulation architecture.** (`stylize.py`) — Refactor the look registry so a look is a small spec {cube, halation?, grain?, vignette?, density?} applied in fixed order at the configured opacity; the still and video paths both walk the stack (video bakes the cube + composites the texture ops). Keep --look-opacity 0.7 default.  _ev: film-emulation-16-35mm 00:34/16:24 (film look = ordered toggle-able node graph, decomposes, not a cube); mostyn-perfect-node-tree (FLC + glow + grain stages); 2hr-beginner-course canonical tree_

## P3  (2)

- **[partial] Per-camera input transform is the ONLY thing that changes per source: a camera->input-LUT lookup (iPhone/consumer = Rec.709 passthrough; Insta360 = I-Log LUT).** (`correct.py`) — If phone/other-camera b-roll grows, add a small camera->input-LUT map and detect source via metadata/filename, defaulting unknown SDR to Rec.709 passthrough (no log decode).  _ev: best-node-tree-any-camera 16:36/21:18 (iPhone/consumer = Rec.709/Rec.709, only node 01 changes per camera); grading-too-complicated 02:26_
- **[partial] Output gamma is a disputed knob: 2.2 (Mullins, web/SDR) vs 2.4 (calibrated monitor). For IG delivery 2.2 is the better default but expose it.** (`tonemap.py`) — If a 2.4-tagged delivery is ever needed, expose an output-transfer flag (srgb/2.2/2.4) on the OETF; default stays sRGB. Document that 2.2 is intentional for IG/SDR web.  _ev: mullins-grading-philosophy 02:59 (delivers Gamma 2.2, not 2.4); best-node-tree 01:51 (2.4 for calibrated monitor)_

## Flagged for human re-check

_6 claims — verify against the cited frame before porting any number (Resolve scale confusions / process notes)._

- 2hr-beginner-course identity defaults list Gain 1.00 AND Offset 25.00 simultaneously (02:03) — in Resolve's Log/Offset readout 25.00 is the NEUTRAL offset (not an additive lift), so 'Offset 25 = unity' should be re-confirmed against the cited frame before treating 25 as a black-floor LIFT in any port.
- film-emulation-16-35mm 10:00 exposure move 'pull Gain 25.00 -> 13.25 (R/G/B)' reads Gain on a 0-25/offset-style scale, not the usual ~1.0 multiplier scale — verify whether this is the Offset wheel (0-100, neutral 25) mislabelled as Gain, since 'Gain 25' is not the standard 1.0-centered gain.
- frenchie BALANCE warmth 'Blue offset down B/Ofs = -0.01' (12:19) is a suspiciously tiny number to produce a visible warm shift on a 0-100/neutral-25 offset scale — likely a wheel-coordinate (-1..1) not the 0-100 readout; re-check the frame so a port doesn't apply a 100x-too-small blue trim.
- mullins claims default Pivot 0.435 = 18% middle-grey in LOG and proves it with a DCTL (21:05); other videos use pivot 0.435 while grading under a 709 output and grading-too-complicated says 0.435 is WRONG for DWG (use 0.336) — the exact working space each 0.435 is measured in is inconsistent across videos and worth one human pass before hard-coding any encoded-space pivot (colorkit's linear 0.18 sidesteps this).
- qualifier-tricks 06:29 final warm-offset 'Offset R 0.47 / G 29.29 / B 53.88' — R at 0.47 on the same scale where G/B are ~29-54 implies red is crushed near zero, an extreme split that contradicts a 'warm/orange lift'; verify the frame (likely a per-channel wheel value vs the 0-100 offset readout being mixed).
- MERGE-PROCESS FLAG (2026-06-26): the merge step was invoked with an EMPTY new-sheet list and 'undefined' source paths because handoff step 4 (vision analysis) had not produced per-video sheets for the 10 newly acquired videos (see gap_map row 'pending-vision-analysis-10-videos'). No demonstrated parameters were invented; synthesis_v2.md and this map were extended with the pending-work record only. Re-run step 4 then re-merge to incorporate real demonstrated values.
