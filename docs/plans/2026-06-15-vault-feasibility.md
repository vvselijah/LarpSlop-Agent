# Vault feasibility mine — 2026-06-15 (Wave 2)

> **What this is.** A per-item feasibility pass over **every idea and every saved repo** in
> Elijah's Obsidian vault. The vault is READ-ONLY — this doc lives in `docs/plans/`, nothing
> was written inside `obsidian/`. Grounded on the actual note bodies (read this run) plus the
> two prior planning docs (`2026-06-15-overnight-program-v2.md` rubric/triage,
> `2026-06-14-overnight-roadmap.md` verdicts).
>
> **Rubric (per idea):** Verdict ∈ {Build-now 🤖 / Phase-0 / Needs-Elijah ⚑ / Venture-scale /
> Content-only / Skip} · Effort · Needs (data/API/dep/decision) · Helpful repo or tool ·
> Composes-with (existing hub engine/skill).
>
> **Lessons carried from the prior runs (applied to every row below):**
> 1. *A vault note's framing is a hypothesis, not the scope* → find the cheapest slice.
> 2. *Split at the measure/generate seam* — measuring composes cheaply; generating/publishing
>    collides with Standing Rule 1 and waits.
> 3. *Verify an API exposes a named metric before promising it* (omnipresence spend/reach, trial-reel
>    detection, skip-rate were all checked this way previously).
> 4. Items the 2026-06-14 roadmap already SKIP'd are marked **"SKIP (prior verdict)"** and not re-litigated.

---

## (1) IDEAS table — every vault idea

Source-note key: `InMoment` = `Brainstorming/In the moment ideas.md` · `AgentTeam` =
`40-Projects/LarpSlop/What I need to do or start with the ai agent team.md` · `DiffVideo` =
`20-Content/Ideas/Different cool video ideas.md` · `Labeltrust-up` =
`40-Projects/Labeltrust/upgrades and ideas or fixes.md`.

| # | Idea (1 line) | Source note | Verdict | Effort | Needs (data/API/dep/decision) | Helpful repo or tool | Composes-with |
|---|---|---|---|---|---|---|---|
| 1 | Kajabi-replacement course+community+gamification site on Archetype Index | InMoment / `Archetype-Index/To do` / `Full self built community website` | **Needs-Elijah** ⚑ (self-build = Venture-scale; SKIP prior verdict) | XL self-build; S landing-page slice | DECISION: buy (Whop/Skool) vs self-build; gamification is load-bearing → Whop has none, points to Skool/hybrid | — (OSS LearnHouse/ClassroomIO = AGPL Docker+Postgres, OneDrive-hostile) | `meta-ads` pixel (funnel tracking) + planned DM-routing inbound feed |
| 2 | Open-source app to fully track Meta stats (Obsidian link, refresh, breakdown, dashboard) | InMoment / `LarpSlop/What I have so far` | **Phase-0** (slices A+B; SKIP SaaS slice C) | A ~1 day / B ~30min / C SKIP | gitignore real account data; audit `vendor/` licenses; README must teach 50–55d token refresh; MIT | own `ig-dashboard/` (~80% built) | `ig-dashboard/refresh.py` + `metrics2026.py` + `dashboard.html` |
| 3 | Mass ad-creative generation for Infinet/Artifacial/ClipWith | InMoment / `Artifacial/To do` | **Needs-Elijah** ⚑ (Phase-0 slate brain buildable now; render loop spend-gated) | P0 slate brain ~½-day | ad spend turning on (`AdSpend/` EMPTY); Mar-2026 "AI-generated" label rule | Higgsfield Marketing Studio + ElevenLabs + `virality_predictor` (all wired) | Claude `--provider agent` (auto-clip idiom); pairs w/ meta-andromeda-ads-playbook |
| 4 | Find a product for Elijah + Lexi to dropship w/ AI character (resin sculptures, arts&crafts) | InMoment | **SKIP (prior verdict)** — venture-scale, weak hub fit; only content-spike salvageable (ADD-LATER) | SKIP (venture XL) | storefront/supplier/fulfillment/CS/ad spend/2nd account — all OUTSIDE hub | Higgsfield Soul-ID + ElevenLabs (assets only) | Soul-ID + auto-clip + caption-engine (assets only) |
| 5 | Connect phone photos into Claude Code for content from old media | InMoment | **Needs-Elijah** ⚑ (cheapest-slice exists but needs a decision on ingest path) | P0 ~½-day for a manual folder-index slice | DECISION: how photos reach disk (manual export folder vs cloud API); no clean keyless iOS Photos API on Windows | `opendataloader-pdf` (n/a here); a simple stdlib EXIF/index walker | composes-with `auto-clip`/`carousel-builder` (source picker) once a photos folder exists |
| 6 | Fully edit the entire Derwin Scott podcast | InMoment / `30-People/Derwin Scott/…` | **Build-now** 🤖 (the editing path is fully built) | M (a real edit session, no new code) | the raw multicam footage (lives in `..\abc wrap\`) | — (skills, not a repo) | `podcast-interview-editor` + `auto-clip` + `caption-engine` + `platform-exporter` |
| 7 | Checkout betta/miro fish repo + OASIS model, integrate into agent team | InMoment | **Phase-0** (OASIS = the already-approved audience-sim spike; "betta/miro" unidentified) | P0 spike already planned ~per audience-sim doc | UNPROVEN for IG until backtested vs 300-post history; clarify "betta/miro fish repo" w/ Elijah | OASIS (social-sim) — see `docs/plans/2026-06-13-audience-sim-pipeline.md` | pre-publish hook-ranker feeding `metrics2026` + carousel-builder |
| 8 | Proprietary ML behavior-prediction model via local LLM-training repos | InMoment | **Phase-0** (literal LLM-training framing declined; sklearn spike, likely ships nothing) | P0 spike ~2h; productionize only if it BEATS baseline | only pre-publish features (no leakage); honest gate vs metrics2026 + Claude-as-evaluator | sklearn `HistGradientBoostingRegressor` (already installed); **NOT** train-llm-from-scratch | `metrics2026.score_2026()` as `y`; lives as `ig-dashboard/predict.py` |
| 9 | Artifacial workshop ad-idea creation (one viral concept per new tool) | InMoment / `Artifacial/To do` | **Build-now** 🤖 (Wave-3 build #2; decoupled from spend) | P0 ~30min / v1 ~½-day; zero deps | finite tool list (`intel/artifacial-tools.json`); n≥5 before trusting niche pacing | own `intel/artifacial-tools.json` + a new SKILL.md | `team/stats.md` + `watchtime_ideator` block + `metrics2026` skip-rate → `20-Content/Ideas/` |
| 10 | Use content-analysis system for long-form ideas (longest-watch-time categories → longform) | InMoment / `Longform content ideas` | **Build-now** 🤖 — ALREADY SHIPPED as `watchtime_ideator.py` (extend, don't rebuild) | XS (extend existing) | n≥5 per category before crowning; data already on disk | own `ig-dashboard/watchtime_ideator.py` | `weekly-content-plan` Step-3 arbitrage + `content-intel-2026` skill |
| 11 | ManyChat alternative / local DM responder (answer all DMs as me, at volume) | InMoment | **Needs-Elijah** ⚑ (~30min Phase-0 unblocks it; then ADD-LATER skill) | P0 ~30min (app-config) / skill S–M | enable messaging product + `messages` webhook on `mybrain` app; one live human-approved send | **NOT** OpenWA (WhatsApp, wrong platform) / NOT Chatwoot/insta-p8 (auto-send) | `comment-triage` pattern (~90% reuse) + `instagram` MCP `get_conversations`/`send_dm` |
| 12 | Reverse-engineer omnipresent top-tier ad strategies | InMoment | **Phase-0** (standalone ad-spy skill now; spend/reach NOT exposed by API → longevity proxy) | P0 ~½-day; zero deps | API does NOT expose commercial-ad spend/reach (only political+EU/DSA) → rank by longevity/variation/density | `meta-ads` `ads_library_search` (LIVE) | `--provider agent` brain; discovery front-end → meta-andromeda-playbook → mass-ad-generator |
| 13 | News update system — unbiased fact-based AI/industry news → vault on interval | InMoment | **Build-now** 🤖 — ALREADY SHIPPED as `intel/news-radar.py` (committed `800058e`) | DONE; wire output into idea-notes | gov/primary-source allowlist; per-feed validation + dead-feed log (already handled) | own `intel/news-radar.py` + `news-sources.json` | `Daily Agent Refresh.bat` → `20-Content/Ideas/` → weekly-content-plan/carousel-builder |
| 14 | (dup of #10) content-analysis → longform feeder | InMoment | **Build-now** 🤖 (= #10, watchtime_ideator) | — | — | own `watchtime_ideator.py` | content-intel-2026 |
| 15 | Build something that sells to AI agents ("SaaS for agents") | InMoment / `Longform content ideas` | **Content-only** (series = ADD-NOW; literal agent-SaaS product = Venture-scale/SKIP) | XS content note; product = SKIP | re-verify 2026 rails before posting; drop "rebuild every SaaS" VC thesis | — (a vault idea-note + verified rails) | `carousel-builder` (untapped saves) + `weekly-content-plan` + `reel-analytics` |
| 16 | Host in-person speaking events (influencers drop free game) | `Hosting In person events` | **Needs-Elijah** ⚑ (logistics-gated; no hub build) | n/a (event logistics) | venue, cast, dates, release forms | — | announce/clip path: auto-clip + caption-engine + carousel-builder |
| 17 | "Meet your cofounder" event — early-2000s game-show 3-ep trilogy | `Meet your cofounder` / `Hosting In person events` | **Needs-Elijah** ⚑ (ADD-LATER format-bible note; prep-skill gated on shoot date) | P0 ~1h format-bible note | event date/cast/release forms; on-camera consent; IP-exposure in pitch segment | — (media path already covered) | auto-clip + caption-engine + carousel-builder + Higgsfield + HyperFrames registry |
| 18 | Live "drop your software, we grade it" show w/ Tanner (rubric: quality/perf/virality/looks/pro-opinion) | `ideas for us going live` | **Build-now** 🤖 (Wave-5 ADD-NOW: rubric + show-prep skill, draft-only) | P0 ~1.5–2.5h; zero deps | honesty gate (grade the work not the person; opinion-frame; every low score → a fix) | new `.claude/skills/live-software-review/SKILL.md` (clone carousel-builder) | WebFetch + `instagram` `get_comments` + `team/stats.md` → scorecards + idea-note |
| 19 | Personal-name SEO/authority for "Elijah Sullivan" + "Tanner Carlson" (rank our results, published articles, AI blog) | AgentTeam | **Phase-0** (the cheapest slice = an auto-updated public progress blog; press = Needs-Elijah) | P0 ~½-day for the blog-generator planner; PR outreach = ⚑ | DECISION: where the blog is hosted (the publish surface is OUTSIDE the hub); HARO/press needs human outreach | a static-site generator + the daily-journal entries as source | the daily-journal/HANDOFF history + `news-radar` (topical hooks) |
| 20 | Viral scraper — top posts per niche + WHY they win + iterate | AgentTeam | **Build-now** 🤖 (Phase-1 worthwhile half) — partly shipped as `intel/viral-radar.py` | P1 ~½-day | use `top_media` proxy → `business_discovery` for true views; NO scraper | own `intel/viral-radar.py` + `competitor-radar.py` | `metrics2026` contract + carousel-builder + weekly-content-plan |
| 21 | Fully automated clone account (remix winners, one niche, more educational) | AgentTeam | **SKIP (prior verdict)** — literal automated clone; Apr-2026 originality policy kills it | SKIP (multi-day + 2nd-account infra) | 2nd account/token; manual proof of 15–20 posts first | — | only the idea→niche-script sliver is useful for the MAIN account |
| 22 | Build full long-form structure → edit → publish polished (inverse of auto-clip) | AgentTeam | **Phase-0** (storyboard brain only; full render demand-gated) | P0 ~½–1 day | a concrete long-form deliverable to justify P1+; account is Reels-first | OTIO (Apache-2.0) for a *later* EDL phase only | clones auto-clip pattern → Remotion in `..\abc wrap\`; edit-video/broll-inserter/caption-engine |
| 23 | Always-on agent — constant research on top carousels/educational videos → auto-replicate+post | AgentTeam | **Phase-0** (scheduled loop ~80% exists; gate on a week of on-demand use) | P1 ~½-day | GATE: a week of `niche-intel` showing Elijah acts on the ideas; never autoposts | own `competitor-radar.py` + `niche-intel` skill | carousel-builder + auto-clip + `Daily Agent Refresh.bat` |
| 24 | Checkout gethookd.ai for borrowable features | AgentTeam | **SKIP (prior verdict)** — DTC ad SaaS, category mismatch; hub replicates 85% free | 0 (skip) | — | — (do NOT subscribe/clone MCP) | hub already covers via competitor-radar + ads_library_search + vault Hooks/ |
| 25 | Integrate ABC wrap (Remotion render farm) into the hub for ad/post creation | AgentTeam / `LarpSlop/ABC wrap` | **Phase-0** (the 41-skill+MCP wiring is ALREADY shared; only a spec.json seam missing) | P0 ~1 session contract doc | gitignore `jobs/*/out/`+`assets/`; stage renders OFF synced disk; golden-rule wall | own `..\abc wrap\` render farm (Remotion `--props`) | spec.json handoff → `HubPostEdit.tsx` (P1) → render-bridge skill (P2) |
| 26 | Labeltrust: input IG/YT/FB/TikTok ad link → analyze the product (dropship? affiliate? baseless claims? cheaper alt?) | Labeltrust-up | **Phase-0** (standalone "social-ad analyzer" skill, read-only) | P0 ~½-day; zero deps | a way to fetch the linked video/page (WebFetch + video-analyzer MCP); Labeltrust is its own repo | `video-analyzer`/`gemini-video` MCPs (wired) + WebFetch | `--provider agent` analyzer skill; outputs a report, not a vault type |
| 27 | Labeltrust: input a URL/product site → return info on it | Labeltrust-up | **Phase-0** (subset of #26; the URL-intake slice) | P0 ~2h | WebFetch + an ingredient/claims rubric | WebFetch + `opendataloader-pdf` (if PDFs/specs) | same analyzer skill as #26 |
| 28 | Labeltrust: massive ingredient×age harm research vault (per-ingredient articles) | Labeltrust-up | **Venture-scale** (Labeltrust product feature; hub can ASSIST research, not own the corpus) | L (research corpus); hub assist = M | authoritative sourcing (peer-reviewed, not conspiracy); this is Cruncrr/Labeltrust's DB | `opendataloader-pdf` (ingest research PDFs → markdown) + `research-lean` workflow | `research`/`research-lean` workflows feed the corpus; data lives in the product repo |
| 29 | Labeltrust: "speculation meter" — legitimacy score for a trend/claim, fact-only | Labeltrust-up | **Phase-0** (standalone "claim-legitimacy" analyzer skill; same engine as #26) | P0 ~½-day; zero deps | a transparent scoring rubric; "fact vs opinion" gate; 50-50 cases need a rule | WebFetch + `research-lean` for citations | same analyzer skill family as #26/#27; outputs a scored report |
| 30 | Labeltrust UX/feature set: crunchy-mom aesthetic, parent resources tab, clickable ingredients, ingredient wiki, color modes, pro-tier features, ADA | Labeltrust-up | **Venture-scale** (product UX backlog — belongs to the Labeltrust repo, not the hub) | L (product work) | this is app dev in `labeltrust` repo; hub has no engine for it | — | none (product backlog; hub can draft copy/research only) |
| 31 | Labeltrust ingredient-double-listing bug fix | Labeltrust-up | **Venture-scale** (a product bug in the Labeltrust repo) | S (in the product repo) | access to the Labeltrust codebase | — | none (not a hub item) |
| 32 | Labeltrust: aesthetically efficient scanned-ingredient display (good/ok/bad) | Labeltrust-up | **Venture-scale** (product UI in the Labeltrust repo) | M (product) | Labeltrust codebase | — | none |
| 33 | Labeltrust: check pricing model vs commonsensemedia.org / Yuka | `Labeltrust/Rebecca's ideas` | **Needs-Elijah** ⚑ (a research+decision, no build) | S (1 research pass) | a pricing decision by Elijah/Rebecca | `research-lean` workflow | `research` workflow (competitor pricing scan) |
| 34 | Trial-reel A/B method (same overlay on many pics / many overlays on one pic) | `Trial reel methods` | **Build-now** 🤖 (Wave-3 build #1; MEASURE half) — ADD-NOW | P0 ~1–2h / P1 ~½-day | `media_product_type` add to refresh.py; caption-tag grouping PRIMARY; reach-floor honesty gate | own `ig-dashboard/trial_ab.py` (imports `metrics2026.rank_posts()`) | `metrics2026` + `experiments.json` + `#abx_` tag → `20-Content/Ideas/` |
| 35 | Social-followers-IRL visual reel (10→100k crowd scale-reveal) for the 100k milestone | DiffVideo | **Build-now** 🤖 (Wave-6 ADD-NOW; HARD DEADLINE — value decays past 100k) | P0 ~1–2h / P1 ~½-day | DEADLINE (ships before 100k); pure WebGL, no torch/cv2/keys | own `three` skill `InstancedMesh` in HyperFrames; Higgsfield/Seedance optional hero shot | HyperFrames + caption-engine + viral-shortform-2026 + platform-exporter → `out/` |
| 36 | Goofy pregnancy "sperm cell wins the war / chosen one" animated video | DiffVideo | **Content-only** (a one-off creative concept; route to the editing suite) | M (a creative build) | a script + assets; Elijah's call on whether to make it | Higgsfield (generate) + HyperFrames | higgsfield-generate + edit-video + caption-engine |
| 37 | Infinet reel: someone with a crazy weapon / "built it with AI" punchline | `Infinet/Ai videos for infinetai.org` | **Content-only** (idea note is incomplete — punchline TBD) | S–M (a creative build) | finish the punchline; Elijah's call | Higgsfield (generate) | higgsfield-generate + edit-video + caption-engine |
| 38 | "Quick funnies" — 100%-output-then-crash burnout joke | `Quick funnies` | **Content-only** (a hook/script seed) | XS | — | — | carousel-builder / weekly-content-plan / caption-engine |
| 39 | "Quick talking videos" — top-5 OSS repos to supercharge Claude Code | `Quick talking videos` | **Content-only** (AI/Tech talking-head seed; on-niche) | XS–S | a verified repo list (the saved-repo table below seeds it) | — | weekly-content-plan + reel-analytics (AI/Tech framing) |
| 40 | "Yaptalks" — Anthropic/AI-vs-surveillance/AI-progression opinion takes | `Yaptalks` | **Content-only** (opinion hooks; sensitive framing — vet before posting) | XS–S | editorial judgment on the political/Anthropic claims before posting | — | weekly-content-plan + caption-engine; reel-analytics for AI/Tech |
| 41 | Longform "why SaaS for AI agents" video (rewrite/iterate a referenced script) | `Longform content ideas` | **Content-only** (= #15's content half; longform format) | S (script work) | re-verify the agent-economy rails | — | carousel-builder + weekly-content-plan; same source as #15 |
| 42 | "Zombie GPT" — swarm of models spreading misinfo to degrade ChatGPT | `Dumb shit/Creating zombie gpt` | **Skip** (flagged "dumb shit" by Elijah; abusive/ToS-hostile — do not build) | 0 | — | — | none |
| 43 | Clipping campaign — build clip folder → send to organictrafficfunnel.com partners | `To do general/Clipping` | **Build-now** 🤖 (Wave-4 ADD-NOW: the `library.py` packaging layer over auto-clip) | P0 ~½-day; zero deps | `--dest` OUTSIDE OneDrive (avoid double-sync); deliver full-res via Dropbox, never chat media | own `auto-clip/library.py` (`shutil.copy` + niche tag + `_index.csv/.md`) | auto-clip engine → `clip-library/<niche>/`; grounded by clipping-agency-research decision |
| 44 | Re-upload all music to Spotify/YT Music (lost in UnitedMasters→DistroKid switch) | `To do general/Upload all my music again` | **Needs-Elijah** ⚑ (manual distributor task, not a hub build) | M (manual) | ISRCs, artwork, details; a DistroKid login (his) | — | none (hub can draft a checklist/metadata sheet at most) |
| 45 | Hire a VA to route Archetype-Index clients through the DMs | `Archetype-Index/To do` | **Needs-Elijah** ⚑ (dm-routing skill; strictly gated on DM Phase-0 send) | S once DM send unblocked; zero deps | gated on #11's Phase-0 send; routing = REPLY in-thread within 24h, NEVER cold outbound | `comment-triage`/dm-triage pattern + `instagram` MCP | the vault IS the CRM (lead qualification in note BODY, no "lead" type) |
| 46 | Champagne Lance collab — promo reactions on Artifacial/Infinet/page/ClipWith, course integration | `30-People/Champagne lance` | **Needs-Elijah** ⚑ (outreach/relationship + a paid-deal decision) | n/a (outreach) | Elijah's outreach + deal terms ($1.5–2.5k/video reference); creative-control terms | — | sponsor-deal template (vault) if it closes |
| 47 | "Artifacial: post daily fresh ad content + build advertisement automations" | `Artifacial/To do` | **Needs-Elijah** ⚑ (= #3/#9 cluster; the "automations" half is spend/publish-gated) | P0 = #9 ideator (now) | publish cadence is Standing-Rule-1 gated; spend for paid | own `intel/artifacial-tools.json` + ideator skill (#9) | artifacial-workshop-ad-ideator (#9) covers the idea half with zero spend |
| 48 | Connect Meta Ads MCP + dashboard + content analyzer + autoposter pipeline ("what I have so far") | `LarpSlop/What I have so far` | **Build-now** 🤖 — mostly DONE (MCP+dashboard+analyzer live); autoposter = SKIP (Rule 1) | DONE except autoposter (won't build) | autoposter violates Standing Rule 1 → never auto-publish | own `ig-dashboard/` + `meta-ads`/`instagram` MCPs | metrics2026 + content-intel-2026 (the analyzer/strategist half) |
| 49 | Time-capsule pitch / homework / confessional segments (event format internals) | `Meet your cofounder` | **Needs-Elijah** ⚑ (= #17 sub-details; logistics-gated) | n/a | folds into #17's format-bible | — | same as #17 |
| 50 | Personal "build-in-public AI brand" content angle (the Lexi/AI-character content slice) | InMoment (#4 content half) | **Content-only** (the salvageable slice of #4 — a content series, ADD-LATER) | XS–S content note | consent/voice-clone gates if Lexi's likeness used | Soul-ID + ElevenLabs (consent-gated) | carousel-builder + weekly-content-plan |

**Verdict counts (50 ideas):**
Build-now 🤖 = **11** (#6, 9, 10, 13, 14, 18, 20, 34, 35, 43, 48) ·
Phase-0 = **11** (#2, 5, 7, 8, 12, 19, 22, 23, 25, 26, 27, 29 — note #5 and #29 split-counted below) ·
*(precise: Phase-0 = #2,5,7,8,12,19,22,23,25,26,27,29 = 12)* ·
Needs-Elijah ⚑ = **13** (#1, 3, 5*, 11, 16, 17, 33, 44, 45, 46, 47, 49 — #5 listed under Phase-0; effective ⚑ = 11: #1,3,11,16,17,33,44,45,46,47,49) ·
Venture-scale = **4** (#28, 30, 31, 32) ·
Content-only = **9** (#15, 36, 37, 38, 39, 40, 41, 50 = 8; +#41 → 8) ·
Skip = **4** (#4 SKIP-prior, #21 SKIP-prior, #24 SKIP-prior, #42 Skip-joke).
*(Overlaps: #5 is Phase-0 pending one decision; #14≡#10; #41 is #15's longform twin; #47≡#3/#9. The clean tally:
**Build-now 11 · Phase-0 12 · Needs-Elijah 11 · Venture-scale 4 · Content-only 8 · Skip 4 = 50.**)*

---

## (2) SAVED REPOS table — the "Cool repos to checkout" note + others

Every repo/tool named in `40-Projects/LarpSlop/Cool repos to checkout.md`, web-verified this run.
Already-judged repos carry their prior verdict; the new ones were checked against the hub's hard
constraints (Windows + OneDrive-synced tree, no multi-GB/torch deps, stdlib-first, official-API-over-scraper,
Standing Rule 1, private-repo license hygiene).

| Repo | What it does | Helps which hub goal | ADD / LATER / SKIP | Reason + cheapest integration path |
|---|---|---|---|---|
| **EvoMap / evolver** (`EvoMap/evolver`, evomap.ai) | "GEP-powered self-evolution engine for AI agents" — Node ≥18 CLI that encodes agent experience as versioned **Genes/Capsules/Events** (prompt+config assets), recursively evolves prompts under the Genome Evolution Protocol, with validation/review gates; optional connect to the EvoMap **Hub network** (leaderboards). 8.7k★. **Evolves prompts/config ONLY — does NOT train weights.** | The just-built **self-improve** skill (`self-improve/grade.py`) — "agentic recursive learning" | **SKIP (adopt the pattern, not the code)** | **Two hard dealbreakers: (1) GPL-3.0-or-later AND "core engine modules distributed in OBFUSCATED form"** — you can't read/audit the engine (violates the hub's verify-before-trust + stdlib-first ethos), and GPL-3 would contaminate this private repo if integrated. (2) It's a Node network/leaderboard ecosystem play, foreign to the hub's stdlib-Python engines. **BUT it CONCEPTUALLY VALIDATES the self-improve design**: versioned prompt/config assets + validation/review gate BEFORE applying + "don't deploy as a live unattended patcher" = exactly self-improve's *propose-via-`docs/plans/`-diff, never auto-apply, dev-workflow-gated* golden rule. **Cheapest path: graft ONE idea into self-improve's SKILL.md** — give accepted proposals a stable ID/version ("Gene"-like) so L6 self-review can track which proposals were accepted and whether they improved the next grade. Zero code from EvoMap; just the bookkeeping discipline it names. Do NOT install, do NOT join the network. |
| **opendataloader-pdf** (`opendataloader-project/opendataloader-pdf`) | Fastest PDF→Markdown/JSON/HTML parser; ~100 pg/sec CPU, OCR 80+ langs, #1 on PDF-to-MD benchmarks (0.907 acc). 8.6k★, **Apache-2.0**. **Caveat: it's a Python wrapper around a JAVA CLI → needs a JRE.** | Labeltrust research-vault ingestion (#28), any doc/spec intake (#27), research workflows | **LATER** | Genuinely best-in-class + permissive license + CPU-only (no torch/cv2/GPU, no OneDrive heavy-import hang). The ONE friction vs "light dep": it's not pure-pip — it shells out to a **Java** CLI, so it needs a JRE on the box (verify before promising "zero-dep"). **Cheapest path: defer until a concrete PDF-ingestion need lands** (Labeltrust ingredient-research corpus #28, or a research task with PDF sources). When it does: `pip install opendataloader-pdf`, confirm a JRE is present, run on ONE PDF off the synced disk first. No need to add it speculatively — `research-lean` + WebFetch cover today's needs. |
| **OpenWA** (`rmyndharis/OpenWA`) | Self-hosted **WhatsApp** API gateway — REST + webhooks + React dashboard, multi-session, one Docker command, SQLite/Postgres pluggable. MIT. | The DM-responder thesis (#11) — IF the channel were WhatsApp | **SKIP** | **Wrong platform.** Elijah's DM pain is **Instagram** (#11), and IG DMs are already reachable via the `instagram` MCP (`get_conversations`/`send_dm`) with no server. OpenWA is WhatsApp-only, needs a **Docker daemon + a 24/7 host** (OneDrive/Windows-hostile, same class as the rejected LearnHouse/ClassroomIO stacks), and WhatsApp business automation carries its own ban surface. No WhatsApp motion exists. Revisit ONLY if Elijah opens a WhatsApp channel — then it's a clean MIT option, but host it OFF the synced laptop. |
| **train-llm-from-scratch** (`FareedKhan-dev/train-llm-from-scratch`) | Hand-written PyTorch GPT training (Base→SFT→RM→PPO/DPO→GRPO), single/multi-GPU, educational. | The "proprietary ML model" idea (#8) | **SKIP** | **Wrong tool at n=300.** Prior verdict stands and is reconfirmed: fine-tuning/ training an LLM needs ~1,000+ examples; at n=300 it memorizes and loses to a 30-line sklearn `HistGradientBoostingRegressor` (already installed, zero deps). This repo also pulls **torch** (the explicit OneDrive heavy-import-hang class). Value is purely **educational/content** (a "how LLMs train" explainer for the AI/Tech niche — feeds #39). Do NOT install; do NOT train on it. |
| **motion / motion.dev** (`motiondivision/motion`) | JS/React/Vue animation library (formerly Framer Motion), hybrid engine, 120fps GPU, springs/layout/scroll/timelines. 32k★, MIT. | "3D motion-graphic vids with Claude" | **SKIP (redundant)** | The hub already has a deep, render-deterministic motion stack purpose-built for video: **HyperFrames** + its adapter skills (`gsap`, `animejs`, `waapi`, `css-animations`, `three`, `typegpu`, `lottie`) + Remotion. motion.dev is a great *web-app* animation lib but it's **not seek-deterministic for frame-accurate MP4 render** the way HyperFrames requires — adding it would fragment the motion toolchain the CLAUDE.md "don't re-litigate" rule guards against. For "3D motion graphics," the `three` skill (already used by the #35 milestone reel) is the right tool. No add. |
| **codecrafters build-your-own-x** (`codecrafters-io/build-your-own-x`) | Curated list of "build your own \<X\> from scratch" tutorials. | "build my own version of anything?" | **SKIP** | It's a **link directory**, not a tool/dependency — nothing to install or integrate. Pure inspiration/learning resource. If a "build X from scratch" explainer is ever wanted for AI/Tech content, it's a content reference (feeds #39's repo-roundup genre), not a hub component. No add. |
| **ASCILINE** (`YusufB5/ASCILINE`) | Python/FastAPI backend (OpenCV+NumPy) streams video as binary-packed ASCII/colored-block frames over WebSockets to a Canvas — "unblockable" 360p@30fps, bypasses ad/video blockers. | (none clear — novelty) | **SKIP** | Novelty with a borderline use-case (its headline pitch is bypassing ad-blockers / "unblockable ads" — reputationally and ToS-hostile, off-brand for an organic creator). Pulls **OpenCV** (cv2 = the OneDrive heavy-import-hang class) + a FastAPI server. No content, intel, ads, or DM goal it advances. Interesting, not now, likely never. No add. |
| **Telegram-Drive** (`caamer20/Telegram-Drive`) | Tauri/Rust/React desktop app turning a Telegram account into "unlimited" cloud storage (Saved Messages/channels as folders), media streaming. FOSS. | Possible large-media/clip storage (#43 clipping folder) | **SKIP** | Tempting as "free unlimited storage" for the clip library, but: relies on **abusing Telegram's ToS** (storage hijack — bannable, files can vanish), it's an unofficial desktop app (Tauri/Rust build), and the clip-library decision already landed on **Dropbox/Drive** as the agency-standard, reliable handoff. Don't stake a content-delivery pipeline on a ToS-gray storage hack. No add. |
| **CloakHQ / cloakbrowser** (`CloakHQ/cloakbrowser`) | Stealth/anti-detect Chromium fork (49 C++ fingerprint patches), drop-in Playwright replacement, multi-accounting, proxy/cookie isolation, Docker. 13.5k★ (MIT wrapper). | (none — anti-detect automation) | **SKIP** | **Anti-detect / multi-accounting browser** = squarely the kind of platform-evasion tooling that collides with Standing Rule 1's spirit and IG/Meta ToS (its whole purpose is defeating bot detection for multi-account ops — exactly the originality/automation signals IG's 2026 "great purge" flags). The hub does everything via **official consented APIs** (the explicit "official APIs over paid scrapers" constraint). Zero legitimate hub use-case. Hard no. |
| **FinceptTerminal** (`Fincept-Corporation/FinceptTerminal`) | Bloomberg-terminal-style finance app (v4 = C++20/Qt6 GUI; PyPI v2 = DearPyGui GUI). | Finance data for the Money niche | **SKIP (prior verdict)** | AGPL-3.0 + commercial clause; multi-GB pinned C++/Qt toolchain = the OneDrive-hang class; neither artifact is a headless data library. Rescoped to a thin keyless `finance-data/fetch.py` (FRED/World Bank official APIs) IF/when the Money niche wants citable numbers. Don't integrate Fincept. |
| **addyosmani/agent-skills** (`addyosmani/agent-skills`) | 24-skill software-engineering workflow pack (spec-driven dev, TDD, CI/CD, security…). MIT, ~50k★. | "see if any benefit us" | **SKIP (prior verdict)** | A software-eng pack that **duplicates** the hub's `dev-workflow` + `agentic-build-loop` + `context-checkpoint`; ~18/24 skills irrelevant to a creator hub. Graft 2–3 markdown paragraphs at most (doubt-driven-dev → review lens; source-driven-dev → RESEARCH-PROTOCOL). Do NOT install. |
| **github/spec-kit** (`github/spec-kit`) | Spec-Driven-Development CLI (constitution→specify→clarify→plan→tasks→implement). MIT, ~112k★. | "super planning skills?" | **SKIP (prior verdict)** | Near-exact overlap with the hub's `dev-workflow` + PLAN templates (which encode hub law + visual-QA + 3-lens review); installing creates two competing method systems in one OneDrive tree + a known Windows `--here` bug. Graft at most: a "constitution" first-premise row in PLAN.md + a clarify-before-plan gate. Do NOT install. |

**ADD-repo list (this run): NONE add-now.**
- **LATER (1):** `opendataloader-pdf` — best-in-class, Apache-2.0, CPU-only; deferred until a concrete
  PDF-ingestion need (Labeltrust research corpus #28 or a PDF-sourced research task), and gated on confirming a JRE.
- **GRAFT-don't-install (1):** **EvoMap/evolver** — GPL-3 + obfuscated core = unusable as code, but its
  versioned-asset + validate-before-apply pattern validates and lightly sharpens the new `self-improve` skill
  (give accepted proposals a stable versioned ID so L6 self-review can track them). Zero EvoMap code adopted.
- **SKIP (rest):** OpenWA (wrong platform + Docker host), train-llm-from-scratch (wrong tool at n=300 + torch),
  motion.dev (redundant with HyperFrames/Remotion), codecrafters/build-your-own-x (a link list), ASCILINE
  (novelty + cv2 + ad-block-evasion), Telegram-Drive (ToS-gray storage hack), cloakbrowser (anti-detect, ToS-hostile),
  + the three prior-verdict skips (Fincept, addyosmani, spec-kit).

**Specific EvoMap → self-improve verdict (as asked):** It should **inform, not be installed**. The
"recursive learning" tag is real but it's prompt/config evolution with human-gated validation — the same
shape `self-improve/grade.py` + the propose-via-`docs/plans/`-diff golden rule already implement. The single
adoptable nugget is *versioned, auditable proposal assets with a review gate* — graft a stable proposal-ID
convention into the self-improve SKILL.md's L6 self-review loop so it can measure whether accepted proposals
actually improved the next grade. GPL-3 + obfuscated binaries make any code reuse a non-starter for a private,
audit-everything hub.

---

## Top 5 to actually do next (ranked by leverage ÷ effort)

1. **#35 social-followers-IRL milestone reel (Build-now 🤖, DEADLINE).** The only item whose value
   *decays* — it celebrates the ~100k milestone and is worthless a week late. Lightest deps of anything
   (pure WebGL via the `three` skill in HyperFrames; no torch/cv2/keys). Ship the smallest postable
   version BEFORE the milestone, then resume the metric cluster. Stops at a file in `out/`.
2. **#43 clipping-campaign-folder `auto-clip/library.py` (Build-now 🤖).** Pure zero-dep `shutil.copy`
   glue over the *already-built* auto-clip engine; unblocks a LIVE vault to-do (the organictrafficfunnel.com
   campaign) TODAY with the 6 existing test clips. Depends on nothing else. `--dest` OFF the OneDrive tree.
3. **#34 trial-reel A/B measure half `ig-dashboard/trial_ab.py` (Build-now 🤖).** Imports
   `metrics2026.rank_posts()` UNCHANGED — a direct, cheap consumer of the metric contract; answers a
   real question Elijah keeps asking (which overlay/picture wins). Caption-tag grouping primary; honesty
   gate on the reach floor. Generate+publish half stays Rule-1 gated.
4. **#9 artifacial-workshop-ad-ideator skill (Build-now 🤖).** Finite tool list → one analytics-grounded
   ad concept per tool as a vault idea-note, with ZERO ad spend (decoupled from the spend-gated executor).
   The only hard part — analytics-grounded ideation — is the hub's home turf and served by no off-the-shelf
   tool. Re-runnable weekly as Tanner ships tools. (Reads #10's watch-time block.)
5. **#18 live-software-review rubric + skill (Build-now 🤖).** One SKILL.md (cloned from carousel-builder)
   + one vault rubric note (Quality 25 / Looks 20 / Virality 20 / Pro-opinion-fix 20 / Performance 15);
   draft-only, zero deps, removes recurring per-episode friction for the Tanner live show. Bake the honesty
   gate in (grade the work, opinion-frame the subjective, every low score carries a fix).

*Runners-up just below the cut:* the **EvoMap graft** into self-improve's L6 loop (near-zero effort, sharpens
the headline Wave-1 build) and the **#12 ad-omnipresence Phase-0 skill** (read-only creative research, useful
even organic). The two Elijah-gated ~30min unlocks worth doing whenever he's available: **#11 DM Phase-0**
(enable messaging product + webhook → one live approved send) and **#33 Labeltrust pricing research**.
